# Development dependencies for AnonReq
# Pinned exact versions for reproducible builds
# Includes production deps
-r requirements.txt
pytest==9.0.0
pytest-asyncio==1.4.0
hypothesis==6.155.0
respx==0.21.0
