apiVersion: v1
kind: Service
metadata:
  name: {{ include "anonreq.fullname" . }}
  labels:
    {{- include "anonreq.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "anonreq.selectorLabels" . | nindent 4 }}
