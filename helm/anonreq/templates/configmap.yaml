apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "anonreq.fullname" . }}
  labels:
    {{- include "anonreq.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
