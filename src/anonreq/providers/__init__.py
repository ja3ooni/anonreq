"""Provider adapter package for multi-provider LLM support.

Contains the ProviderAdapter ABC, concrete provider implementations
(AnthropicAdapter, GeminiAdapter, OllamaAdapter), the ProviderRegistry
for adapter resolution, and the CapabilityResolver for capability lookup.
"""
