---
phase: 27-v1-5-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/trust_center.yaml
  - .planning/phases/23-engineering-hygiene/23-01-SUMMARY.md
  - tests/test_agent_approval.py
  - tests/test_agent_policy.py
autonomous: true
requirements: ["HYG-01", "HYG-02", "TRUST-01"]
user_setup: []

must_haves:
  truths:
    - "No ModuleNotFoundError is raised during pytest collection"
    - "tests/test_agent_approval.py does not exist on disk"
    - "tests/test_agent_policy.py does not exist on disk"
    - "config/trust_center.yaml has enabled set to true"
    - ".planning/phases/23-engineering-hygiene/23-01-SUMMARY.md accurately describes the global mypy strict = true flag and overrides instead of a staged rollout"
  artifacts:
    - path: config/trust_center.yaml
      provides: "Default configuration enabling Trust Center by default"
      contains: "enabled: true"
    - path: .planning/phases/23-engineering-hygiene/23-01-SUMMARY.md
      provides: "Accurate documentation of code quality and strictness enforcement"
      contains: "strict = true", "mypy"
  key_links:
    - from: config/trust_center.yaml
      to: "src/anonreq/main.py"
      via: "FastAPI startup logic reading YAML config to expose trust center"
---

<objective>
Close three critical technical debt gaps from the v1.5 milestone audit: delete two non-functional agent test files causing CI collection failures, flip the Trust Center configuration to default-enabled, and correct engineering hygiene documentation to accurately reflect the global strictness configuration and module override model.
</objective>

<execution_context>
@/Users/aljaunia/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/aljaunia/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-v1-5-tech-debt-cleanup/27-CONTEXT.md
@.planning/phases/27-v1-5-tech-debt-cleanup/27-PATTERNS.md
@config/trust_center.yaml
@.planning/phases/23-engineering-hygiene/23-01-SUMMARY.md
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete tests/test_agent_approval.py and tests/test_agent_policy.py</name>
  <files>tests/test_agent_approval.py, tests/test_agent_policy.py</files>
  <read_first>
    tests/test_agent_approval.py — inspect imports and verify the non-existent modules 'anonreq.agent.approval' and 'anonreq.agent.inspector' are causing the collection error.
    tests/test_agent_policy.py — inspect imports and verify the non-existent modules 'anonreq.agent.policy' and 'anonreq.agent.registry' are causing the collection error.
  </read_first>
  <action>
    Delete both 'tests/test_agent_approval.py' and 'tests/test_agent_policy.py' files.
    Per D-01/D-02: These files test features that were never built and never existed in git history. Deleting them is the approved way to resolve the CI collection failures.
    Per D-03: Do NOT attempt to build the non-existent 'anonreq.agent.approval/policy/inspector/registry' packages.
    Do NOT skip-mark, exclude via pytest args in CI, or move them to a quarantine directory.
  </action>
  <verify>
    <automated>[ ! -f tests/test_agent_approval.py ] && [ ! -f tests/test_agent_policy.py ] && uv run pytest tests/ --collect-only -q --ignore=tests/load</automated>
  </verify>
  <done>
    Both files are removed from the filesystem and pytest collection completes with zero ModuleNotFoundErrors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Modify config/trust_center.yaml to flip enabled to true</name>
  <files>config/trust_center.yaml</files>
  <read_first>
    config/trust_center.yaml — read the first few lines to find the `enabled: false` setting.
  </read_first>
  <action>
    Modify 'config/trust_center.yaml' to set 'enabled: true'.
    Follow the mapped edits and configuration pattern documented in 27-PATTERNS.md ## config/trust_center.yaml.
    You can optionally add a comment explaining that this can be toggled to false if the operator desires to disable public status endpoints.
    Per D-04/D-05: Exposing the Trust Center by default aligns with its public, no-auth design.
    Per D-06: No other Trust Center behaviors (60 RPM rate-limiting, fail-closed 503s, aggregate metadata-only outputs) are affected.
    CAUTION (Pitfall 1): Do NOT edit 'tests/test_trust_center.py' or any code assertions. The test asserting `TrustCenterSettings().enabled is False` verifies the Python class default fallback behavior when no config file is parsed, which is distinct from the shipped config file and must remain False.
  </action>
  <verify>
    <automated>grep -q "enabled: true" config/trust_center.yaml && uv run pytest tests/test_trust_center.py -q</automated>
  </verify>
  <done>
    `config/trust_center.yaml` contains `enabled: true` and the existing trust center tests pass successfully.
  </done>
</task>

<task type="auto">
  <name>Task 3: Correct .planning/phases/23-engineering-hygiene/23-01-SUMMARY.md</name>
  <files>.planning/phases/23-engineering-hygiene/23-01-SUMMARY.md</files>
  <read_first>
    .planning/phases/23-engineering-hygiene/23-01-SUMMARY.md — inspect the Accomplishments and Summary text to find where quality enforcement is mentioned.
  </read_first>
  <action>
    Correct `.planning/phases/23-engineering-hygiene/23-01-SUMMARY.md` to remove or rewrite claims about how code quality strictness is staged.
    Follow the documentation prose pattern assignment in 27-PATTERNS.md ## .planning/phases/23-engineering-hygiene/23-01-SUMMARY.md.
    Because the literal phrase "staged rollout" does not exist in the file, edit the summary text (specifically lines 40-41 and accomplishments) to explain the actual mechanism:
    1. Ruff enforces a single global rule set (`E, F, I, N, W, UP, B, SIM, ARG, PT, RUF`) uniformly across the codebase with no staged rollout or per-module variation.
    2. Mypy is configured globally with `strict = true` across `src/`, using module-specific `[[tool.mypy.overrides]]` blocks in `pyproject.toml` to disable select error codes or ignore missing imports for first-party and third-party modules. This is a permanent pragmatic override pattern, not a rollout schedule that will be tightened over time.
    Per D-07: The documentation must match reality.
    Per D-08: Do NOT build an actual per-directory strictness staging mechanism.
    CAUTION (Pitfall 3): Describe the overrides mechanism generically; do not imply Phase 23 alone authored the full override list, as first-party overrides were added incrementally in Phases 24-26.
  </action>
  <verify>
    <automated>grep -q "strict = true" .planning/phases/23-engineering-hygiene/23-01-SUMMARY.md && grep -q "overrides" .planning/phases/23-engineering-hygiene/23-01-SUMMARY.md</automated>
  </verify>
  <done>
    `.planning/phases/23-engineering-hygiene/23-01-SUMMARY.md` is updated with accurate descriptions of ruff rules and the mypy strictness config plus overrides.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public Network → trust endpoints | Anyone can access public `/v1/trust/*` endpoints |
| Codebase modification → config files | Changing defaults affects active production configuration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01   | Information Disclosure | Trust Center | mitigate | Trust Center is rate-limited (60 RPM/IP) and returns aggregate metadata only, preventing data exfiltration even when enabled by default (D-06). |
| T-27-02   | Tampering | CI/CD pipeline | accept | Removing non-functional test files is verified to restore clean test collection. Code checkouts are git version-controlled. |
</threat_model>

<verification>
1. Verify both test files are deleted and pytest collects successfully:
`[ ! -f tests/test_agent_approval.py ] && [ ! -f tests/test_agent_policy.py ] && uv run pytest tests/ --collect-only -q --ignore=tests/load`
2. Verify YAML default is enabled:
`grep -q "enabled: true" config/trust_center.yaml`
3. Verify existing trust center tests pass:
`uv run pytest tests/test_trust_center.py -q`
4. Verify document description is accurate:
`grep -q "strict = true" .planning/phases/23-engineering-hygiene/23-01-SUMMARY.md`
</verification>

<success_criteria>
- Broken agent test files `tests/test_agent_approval.py` and `tests/test_agent_policy.py` are deleted (HYG-01).
- `config/trust_center.yaml` default is flipped to `enabled: true` (TRUST-01).
- `.planning/phases/23-engineering-hygiene/23-01-SUMMARY.md` is corrected to accurately describe ruff rules and mypy configuration (HYG-02).
- CI/CD workflow collects tests cleanly with zero ModuleNotFoundErrors.
</success_criteria>

<output>
No additional output files are created by this phase beyond modifying config, deleting test files, and updating the summary doc.
</output>
