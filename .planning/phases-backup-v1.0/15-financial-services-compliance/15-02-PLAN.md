---
phase: 15-financial-services-compliance
plan: 02
type: execute
wave: 2
depends_on: [14, 15-01]
files_modified:
  - src/anonreq/governance/model_inventory.py
  - src/anonreq/governance/provider_inventory.py
  - src/anonreq/governance/forwarding_guard.py
  - src/anonreq/governance/router.py
  - src/anonreq/models/governance.py
  - tests/test_model_inventory.py
  - tests/test_provider_inventory.py
  - tests/test_forwarding_guard_mrm.py
autonomous: true
requirements:
  - REQ-38
  - REQ-40

user_setup: []

must_haves:
  truths:
    - "Model inventory tracks risk classification, approval status, review cycles per SR 11-7"
    - "Unapproved models blocked at ForwardingGuard — no traffic reaches provider"
    - "Third-party provider inventory with DORA ICT concentration risk flagging"
    - "Provider suspension endpoint immediately blocks all traffic to that provider"
    - "CRITICAL providers require annual concentration risk justification"
  artifacts:
    - path: "src/anonreq/governance/model_inventory.py"
      provides: "Model inventory with lifecycle integration and approval gating"
      exports: ["ModelInventory", "create_model_record", "get_model_record", "is_model_approved"]
      min_lines: 100
    - path: "src/anonreq/governance/provider_inventory.py"
      provides: "Third-party provider inventory with DORA ICT risk flagging"
      exports: ["ProviderInventory", "create_provider_record", "suspend_provider", "unsuspend_provider", "flag_concentration_risk"]
      min_lines: 100
    - path: "src/anonreq/governance/forwarding_guard.py"
      provides: "Extended ForwardingGuard with model approval check"
      exports: ["check_model_approval", "check_provider_active"]
      min_lines: 30
  key_links:
    - from: "src/anonreq/governance/model_inventory.py"
      to: "src/anonreq/governance/lifecycle.py"
      via: "Model lifecycle stages use Phase 14 LifecycleManager"
      pattern: "LifecycleManager"
    - from: "src/anonreq/governance/provider_inventory.py"
      to: "src/anonreq/governance/lifecycle.py"
      via: "Provider lifecycle stages use Phase 14 LifecycleManager"
      pattern: "LifecycleManager"
    - from: "src/anonreq/governance/forwarding_guard.py"
      to: "src/anonreq/governance/model_inventory.py"
      via: "check_model_approval queries ModelInventory"
      pattern: "is_model_approved"
---

<objective>
Build Model Risk Management (SR 11-7) and third-party provider inventory (DORA ICT) for Phase 15. This wave delivers: (1) model inventory with Phase 14 lifecycle integration per D-005/D-006, (2) model approval gating at ForwardingGuard per D-007, (3) third-party provider inventory with DORA ICT concentration risk flagging per D-009/D-010, and (4) provider suspension endpoint per D-011.

Purpose: Unapproved models must never reach production. Third-party provider concentration risk must be visible and manageable.

Output: `GET /v1/admin/models` lists model inventory. Unapproved models blocked at ForwardingGuard. `POST /v1/admin/providers/{id}/suspend` immediately blocks all traffic.
</objective>

<execution_context>
@/Users/aljaunia/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/aljaunia/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/15-financial-services-compliance/15-CONTEXT.md
@.planning/phases/15-financial-services-compliance/15-ARCHITECTURE.md
@.planning/phases/15-financial-services-compliance/15-DISCUSSION-LOG.md
@.planning/phases/15-financial-services-compliance/15-SECURITY-ACCEPTANCE.md
@.planning/phases/14-ai-governance-oversight/14-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement model inventory with Phase 14 lifecycle integration</name>
  <files>
    src/anonreq/governance/model_inventory.py,
    src/anonreq/models/governance.py,
    tests/test_model_inventory.py
  </files>
  <behavior>
    - Test 1: create_model_record stores model with risk classification, approval status
    - Test 2: get_model_record retrieves model by id or provider+model_name
    - Test 3: is_model_approved returns True only for APPROVED/PRODUCTION stage
    - Test 4: Model lifecycle uses Phase 14 LifecycleManager stages
    - Test 5: Model supports SR 11-7 fields (risk_classification, documentation, validation_status)
    - Test 6: list_models paginates and filters by risk_classification, provider
  </behavior>
  <action>
    Implement model inventory per D-005, D-006, D-007.

    1. **Add to src/anonreq/models/governance.py**:
       - `ModelRiskClassification` enum: `LOW`, `MODERATE`, `HIGH` (per SR 11-7)
       - `ModelRecord(BaseModel)`: id (str), provider (str), model_name (str), risk_classification (ModelRiskClassification), approval_status (str), current_stage (str="DRAFT"), lifecycle_object_id (str), version (str), documentation_url (str | None), validation_status (str | None), validation_date (datetime | None), review_cycle_days (int=365), last_review_date (datetime | None), next_review_date (datetime | None), created_at (datetime), updated_at (datetime)

    2. **src/anonreq/governance/model_inventory.py**:
       - `class ModelInventory`:
         - `__init__(self, db: AsyncSession, lifecycle_manager: LifecycleManager)`:
           - Stores DB session and lifecycle manager reference
         - `async def create_model_record(self, provider: str, model_name: str, risk_classification: ModelRiskClassification, documentation_url: str | None = None) -> ModelRecord`:
           - Creates model record in PostgreSQL
           - Creates lifecycle object via `lifecycle_manager` (starts at DRAFT)
           - Returns created record
         - `async def get_model_record(self, model_id: str) -> ModelRecord | None`
         - `async def get_model_record_by_name(self, provider: str, model_name: str) -> ModelRecord | None`
         - `async def is_model_approved(self, provider: str, model_name: str) -> bool`:
           - Returns True if model exists and current_stage in ["APPROVED", "PRODUCTION"]
           - Returns False if model doesn't exist (fail-secure: block unknown models)
         - `async def list_models(self, risk_classification: ModelRiskClassification | None = None, provider: str | None = None, skip: int = 0, limit: int = 50) -> list[ModelRecord]`
         - `async def update_model_review(self, model_id: str, validation_status: str, validation_date: datetime) -> ModelRecord`:
           - Updates review fields, sets next_review_date based on interval
         - `@staticmethod` `def get_sr_11_7_alignment() -> dict`:
           - Returns alignment documentation mapping SR 11-7 principles to implementation

    3. **tests/test_model_inventory.py**:
       - Test model CRUD with lifecycle integration
       - Test approval check
       - Test fail-secure: unknown model returns not-approved
       - Test SR 11-7 alignment documentation
  </action>
  <verify>
    <automated>
      python3 -c "
      from anonreq.models.governance import ModelRecord, ModelRiskClassification
      model = ModelRecord(provider='openai', model_name='gpt-4', risk_classification='HIGH')
      assert model.risk_classification.value == 'HIGH'
      print('Model inventory model OK')
      "
    </automated>
  </verify>
  <done>Model inventory with lifecycle integration, SR 11-7 fields, approval gating. Unknown models default to not-approved (fail-secure). All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement model approval gating at ForwardingGuard</name>
  <files>
    src/anonreq/governance/forwarding_guard.py,
    tests/test_forwarding_guard_mrm.py
  </files>
  <behavior>
    - Test 1: Approved model passes ForwardingGuard check
    - Test 2: Unapproved model blocked at ForwardingGuard → HTTP 403
    - Test 3: Unknown model blocked (fail-secure) → HTTP 403
    - Test 4: model_approval_gated audit event emitted on block
    - Test 5: Prometheus metric anonreq_model_approval_gates_total incremented
    - Test 6: ForwardingGuard checks both kill-switch AND model approval
  </behavior>
  <action>
    Extend ForwardingGuard to include model approval check per D-007.

    1. **Extend src/anonreq/governance/forwarding_guard.py**:
       - `async def check_model_approval(model_inventory: ModelInventory, provider: str, model_name: str) -> None`:
         - Calls `model_inventory.is_model_approved(provider, model_name)`
         - If not approved: raises `ModelNotApprovedError`
         - Increments `anonreq_model_approval_gates_total` Prometheus counter with result label
         - Emits `model_approval_gated` audit event
       - Update the ForwardingGuard check sequence: `check_kill_switch` → `check_model_approval` → forward
       - Both checks must pass before any provider traffic is dispatched

    2. **tests/test_forwarding_guard_mrm.py**:
       - Test approval check with mock ModelInventory
       - Test fail-secure: mock returns None for unknown model → blocked
       - Test combined kill-switch + model approval check sequence
       - Test audit event emission
  </action>
  <verify>
    <automated>
      python3 -c "
      from anonreq.governance.forwarding_guard import check_model_approval
      print('ForwardingGuard MRM interface OK')
      "
    </automated>
  </verify>
  <done>ForwardingGuard checks model approval before dispatch. Unapproved and unknown models blocked. Audit events and Prometheus metrics recorded. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement third-party provider inventory with DORA ICT risk + suspension</name>
  <files>
    src/anonreq/governance/provider_inventory.py,
    src/anonreq/governance/router.py,
    src/anonreq/models/governance.py,
    tests/test_provider_inventory.py
  </files>
  <behavior>
    - Test 1: create_provider_record stores provider with DORA ICT fields
    - Test 2: get_provider_record retrieves by id
    - Test 3: suspend_provider sets status="suspended" and blocks traffic
    - Test 4: unsuspend_provider sets status="active"
    - Test 5: flag_concentration_risk sets concentration_risk=True and triggers notification
    - Test 6: list_providers filters by status, concentration_risk
    - Test 7: Provider suspension emits provider_suspended audit event
  </behavior>
  <action>
    Implement third-party provider inventory per D-009 through D-012.

    1. **Add to src/anonreq/models/governance.py**:
       - `ProviderRecord(BaseModel)`: id (str), name (str), provider_type (str), status (str="active"), lifecycle_object_id (str), dora_ict_critical (bool=False), concentration_risk (bool=False), concentration_risk_justification (str | None), concentration_risk_justification_date (datetime | None), contract_end_date (datetime | None), review_cycle_days (int=365), last_review_date (datetime | None), next_review_date (datetime | None), created_at (datetime), updated_at (datetime)

    2. **src/anonreq/governance/provider_inventory.py**:
       - `class ProviderInventory`:
         - `__init__(self, db: AsyncSession, lifecycle_manager: LifecycleManager)`:
           - Stores DB session and lifecycle manager
         - `async def create_provider_record(self, name: str, provider_type: str, dora_ict_critical: bool = False) -> ProviderRecord`:
           - Creates provider record with Phase 14 lifecycle (starts DRAFT)
         - `async def get_provider_record(self, provider_id: str) -> ProviderRecord | None`
         - `async def list_providers(self, status: str | None = None, concentration_risk: bool | None = None, skip: int = 0, limit: int = 50) -> list[ProviderRecord]`
         - `async def suspend_provider(self, provider_id: str, reason: str, suspended_by: str) -> ProviderRecord`:
           - Sets status="suspended"
           - Emits `provider_suspended` audit event
           - All active sessions to this provider are terminated
         - `async def unsuspend_provider(self, provider_id: str, unsuspended_by: str) -> ProviderRecord`:
           - Sets status="active"
           - Emits `provider_unsuspended` audit event
         - `async def flag_concentration_risk(self, provider_id: str, justification: str) -> ProviderRecord`:
           - Sets concentration_risk=True, stores justification
           - Triggers notification (DORA ICT concentration risk)
           - Sets next_review_date to 1 year from now per D-012
         - `async def is_provider_active(self, provider_id: str) -> bool`:
           - Returns True only if status == "active"
         - `async def check_provider_active(self, provider_id: str) -> None`:
           - Used by ForwardingGuard; raises `ProviderSuspendedError` if not active

    3. **Add to router.py**:
       - `GET /v1/admin/providers` — list providers (filterable by status, concentration_risk)
       - `GET /v1/admin/providers/{id}` — get provider details
       - `POST /v1/admin/providers/{id}/suspend` — suspend provider
         - Body: `{"reason": "...", "suspended_by": "..."}`
       - `POST /v1/admin/providers/{id}/unsuspend` — unsuspend provider
         - Body: `{"unsuspended_by": "..."}`
       - `POST /v1/admin/providers/{id}/concentration-risk` — flag concentration risk
         - Body: `{"justification": "..."}`

    4. **tests/test_provider_inventory.py**:
       - Test provider CRUD and lifecycle integration
       - Test suspend/unsuspend flow
       - Test concentration risk flagging with notification
       - Test DORA ICT critical flag
  </action>
  <verify>
    <automated>
      python3 -c "
      from anonreq.models.governance import ProviderRecord
      prov = ProviderRecord(name='OpenAI', provider_type='llm', dora_ict_critical=True)
      assert prov.status == 'active'
      assert prov.dora_ict_critical == True
      print('Provider inventory model OK')
      "
    </automated>
  </verify>
  <done>Provider inventory with DORA ICT fields, suspend/unsuspend, concentration risk flagging. Provider suspension blocks all traffic. All tests pass.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ForwardingGuard → ModelInventory | Model approval status checked per request |
| Provider suspension → Provider dispatch | Suspend state checked before routing |
| Admin API → Model/Provider inventory | CRUD operations via authenticated endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02-01 | Tampering | Model approval gate bypass | mitigate | Inline check at ForwardingGuard — cannot be skipped; fail-secure blocks unknown models |
| T-15-02-02 | Tampering | Provider suspension bypass | mitigate | check_provider_active inline in dispatch path |
| T-15-02-03 | Elevation of Privilege | Admin inventory endpoints | mitigate | Admin role required for all endpoints |
| T-15-02-04 | Information Disclosure | Model/Provider list | mitigate | Admin auth only; tenant-scoped queries where applicable |
| T-15-02-05 | Denial of Service | Provider suspension | mitigate | Auth-protected; audit trail for every suspension action |

</threat_model>

<verification>
1. `pytest tests/test_model_inventory.py -x --tb=short -v` passes
2. `pytest tests/test_forwarding_guard_mrm.py -x --tb=short -v` passes
3. `pytest tests/test_provider_inventory.py -x --tb=short -v` passes
4. ForwardingGuard blocks unapproved model (integration test)
5. Provider suspension blocks all traffic (integration test)
6. Concentration risk flag triggers notification
</verification>

<success_criteria>
- [ ] Model inventory with SR 11-7 fields (risk_classification, approval_status, validation_status)
- [ ] Model lifecycle integrated with Phase 14 LifecycleManager (DRAFT→APPROVED→PRODUCTION)
- [ ] ForwardingGuard checks model approval before dispatch
- [ ] Unknown models blocked (fail-secure)
- [ ] model_approval_gated audit events and Prometheus counter
- [ ] Provider inventory with DORA ICT critical flag, concentration_risk flag
- [ ] Provider suspension endpoint blocks all traffic
- [ ] Concentration risk flagging with justification and notification
- [ ] Provider lifecycle via Phase 14 LifecycleManager
- [ ] Annual review cycle for providers (365 days)
- [ ] All tests pass
</success_criteria>
