---
phase: 12-data-classification-handling
plan: 03
type: execute
wave: 3
depends_on:
  - 12-02-PLAN.md
files_modified:
  - src/anonreq/api/v1/chat.py
  - src/anonreq/services/audit_logger.py
  - src/anonreq/middleware/response_headers.py
  - tests/test_classification_response.py
  - tests/test_classification_audit.py
  - tests/test_classification_properties.py
autonomous: true
requirements:
  - CLASS-05

user_setup: []

must_haves:
  truths:
    - "X-AnonReq-Classification-Result returned only when X-AnonReq-Return-Classification: true"
    - "X-AnonReq-Debug does NOT trigger classification response headers"
    - "Classification_Level in every audit log entry"
    - "Property-based tests prove invariants: monotonic, deterministic, increase-only"
  artifacts:
    - path: "src/anonreq/middleware/response_headers.py"
      provides: "Response header middleware for classification headers"
      exports: ["ClassificationResponseMiddleware"]
      min_lines: 40
    - path: "tests/test_classification_properties.py"
      provides: "Property-based tests for classification invariants"
      contains: "Hypothesis"
      min_lines: 80
    - path: "tests/test_classification_response.py"
      provides: "Response header unit and integration tests"
      contains: "test_"
      min_lines: 60
  key_links:
    - from: "src/anonreq/middleware/response_headers.py"
      to: "src/anonreq/models/processing_context.py"
      via: "Reads ctx.classification_result for response headers"
      pattern: "classification_result"
    - from: "src/anonreq/services/audit_logger.py"
      to: "src/anonreq/models/processing_context.py"
      via: "Reads ctx.classification_result.highest for audit log"
      pattern: "classification"
---

<objective>
Finalize Phase 12 with response headers, audit integration, and property-based tests. This plan delivers: (1) X-AnonReq-Classification-Result response header only when explicitly requested, (2) classification level in every audit log entry, and (3) property-based tests (Hypothesis) proving classification invariants.

Purpose: Auditors need classification in every log entry. Enterprise clients need to see classification results without always receiving them. Property tests mathematically prove correctness.

Output: Response includes classification only when requested. Audit entries always contain classification_level. Hypothesis tests confirm all invariants.
</objective>

<execution_context>
@/Users/aljaunia/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/aljaunia/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/12-data-classification-handling/12-CONTEXT.md
@.planning/phases/12-data-classification-handling/12-ARCHITECTURE.md
@.planning/phases/12-data-classification-handling/12-TEST-PLAN.md
</context>

<interfaces>
<!-- From Phase 12 12-01: -->
<!-- ClassificationEngine, ClassificationLevel, ClassificationResult -->

<!-- From Phase 12 12-02: -->
<!-- Pipeline integration, PDP #2 classification evaluation -->

<!-- From Phase 5 AuditLogger: -->
<!-- Structured JSON logging with field allowlist -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement response headers for classification</name>
  <files>
    src/anonreq/middleware/response_headers.py,
    src/anonreq/api/v1/chat.py,
    tests/test_classification_response.py
  </files>
  <behavior>
    - Test 1: X-AnonReq-Classification-Result present when X-AnonReq-Return-Classification: true
    - Test 2: Header absent when X-AnonReq-Return-Classification is missing
    - Test 3: Header absent when X-AnonReq-Return-Classification is false
    - Test 4: X-AnonReq-Debug alone does NOT produce classification response headers
    - Test 5: Header value contains highest level and entity labels
    - Test 6: Blocked request includes classification in error response body
  </behavior>
  <action>
    Implement classification response headers:

    1. **src/anonreq/middleware/response_headers.py**:
       - `ClassificationResponseMiddleware` reads `X-AnonReq-Return-Classification` from request headers
       - Only adds `X-AnonReq-Classification-Result` header when value is exactly "true"
       - `X-AnonReq-Debug` alone does NOT trigger classification headers (per D-017)
       - Header value: JSON with `highest` level name, `labels` list, `client_override` boolean
       - Uses `request.state.ctx` to access ProcessingContext classification result

    2. **src/anonreq/api/v1/chat.py** — Update route handler:
       - Store `ctx` on `request.state.ctx` for middleware access
       - Blocked requests (HTTP 451): include classification in response body:
         ```
         classification: { highest, labels, reason }
         ```

    3. **tests/test_classification_response.py**:
       - Header present when explicitly requested
       - Header absent when header missing, false, or debug-only
       - Blocked response body includes classification
  </action>
  <verify>
    <automated>
      ANONREQ_API_KEY=testkey1234567890123456789012345678 \
      ANONREQ_VALKEY_URL=redis://localhost:6379 ANONREQ_PRESIDIO_URL=http://localhost:5001 \
      python3 -c "
      from anonreq.middleware.response_headers import ClassificationResponseMiddleware
      print('Response header middleware import OK')
      "
    </automated>
  </verify>
  <done>Classification response headers added only when explicitly requested. X-AnonReq-Debug alone does not trigger classification headers. Blocked requests include classification in error body.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add classification level to audit log entries</name>
  <files>
    src/anonreq/services/audit_logger.py,
    tests/test_classification_audit.py
  </files>
  <behavior>
    - Test 1: Audit log entry contains classification_level when classification exists
    - Test 2: Audit entry contains classification_level: null when no classification
    - Test 3: Entity labels included in audit metadata
    - Test 4: Client override flag in audit metadata
    - Test 5: Classification audit fields contain no raw PII
  </behavior>
  <action>
    Add classification to audit log entries:

    1. **src/anonreq/services/audit_logger.py**:
       - Add `classification_level`, `classification_labels`, `classification_client_override`, `classification_client_asserted_level` to ALLOWED_FIELDS
       - Add `_extract_classification(ctx)` method returning metadata-only dict
       - Include classification dict in every audit event emission
       - Null-safe: returns `{"classification_level": None}` when no classification exists
       - Entity labels included as list (metadata only, no raw values)

    2. **tests/test_classification_audit.py**:
       - Test classification_level present when set on context
       - Test classification_level null when not set
       - Test entity labels in metadata
       - Test no raw PII in audit fields
  </action>
  <verify>
    <automated>
      ANONREQ_API_KEY=testkey1234567890123456789012345678 \
      ANONREQ_VALKEY_URL=redis://localhost:6379 ANONREQ_PRESIDIO_URL=http://localhost:5001 \
      python3 -c "
      from anonreq.services.audit_logger import AuditLogger
      print('AuditLogger with classification import OK')
      "
    </automated>
  </verify>
  <done>Classification level in every audit log entry. Entity labels and client override flag included. Null-safe when classification not set.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Property-based tests for classification invariants</name>
  <files>
    tests/test_classification_properties.py
  </files>
  <behavior>
    - Invariant 1: Classification is monotonic — adding entities never lowers level
    - Invariant 2: Classification is deterministic — same input = same output
    - Invariant 3: Client increase is monotonic — only increases, never decreases
    - Invariant 4: All audit entries contain classification_level field
    - Invariant 5: No PII in classification-related audit fields
  </behavior>
  <action>
    Write property-based tests using Hypothesis:

    1. **tests/test_classification_properties.py**:
       - `test_classification_deterministic(entity_types)` — same input 3x, verify identical ClassificationResult
       - `test_client_increase_only(entity_types, client_level)` — client decrease rejected, increase applied
       - `test_classification_monotonic(entities_a, entities_b)` — superset entity list never lowers level
       - `test_max_algorithm_correctness(entity_types)` — highest equals max of individual entity levels
       - `test_all_audit_entries_have_classification()` — N=500 random pipeline scenarios verify audit field
       - `test_no_pii_in_classification_audit()` — N=500 scenarios verify audit fields match allowlist

       Use Hypothesis strategies:
       ```python
       entity_types = st.lists(
           st.sampled_from(list(ENTITY_CLASSIFICATION_MAP.keys())),
           min_size=0, max_size=20,
       )
       classification_levels = st.sampled_from(list(ClassificationLevel))
       ```

       Cross-request test: verify different sessions with same entity set produce same classification.

    2. Run with statistics:
       - `pytest tests/test_classification_properties.py -x --tb=short -v --hypothesis-show-statistics`
  </action>
  <verify>
    <automated>
      ANONREQ_API_KEY=testkey1234567890123456789012345678 \
      ANONREQ_VALKEY_URL=redis://localhost:6379 ANONREQ_PRESIDIO_URL=http://localhost:5001 \
      pytest tests/test_classification_properties.py -x --tb=short -v --hypothesis-show-statistics 2>&1 | tail -20
    </automated>
  </verify>
  <done>Property-based tests confirm deterministic, monotonic, increase-only invariants. All 5 invariants tested with Hypothesis. N=500 random scenarios pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gateway -> Client | Classification response headers returned to client |
| Gateway -> Audit Log | Classification fields written to structured log |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-01 | Information Disclosure | Classification header returned without request | mitigate | Header only emitted when X-AnonReq-Return-Classification: true explicitly sent |
| T-12-03-02 | Information Disclosure | Classification in audit logs | mitigate | Metadata only — level labels, no raw entities or content |
| T-12-03-03 | Tampering | Property test coverage gaps | mitigate | Hypothesis strategies cover all entity types, combinations, and empty/edge cases |
</threat_model>

<verification>
1. `pytest tests/test_classification_response.py -x --tb=short -v` passes
2. `pytest tests/test_classification_audit.py -x --tb=short -v` passes
3. `pytest tests/test_classification_properties.py -x --tb=short -v` passes
4. Classification header present only on explicit request
5. Audit entries always contain classification_level
</verification>

<success_criteria>
- [ ] X-AnonReq-Classification-Result header added only when X-AnonReq-Return-Classification: true
- [ ] X-AnonReq-Debug alone does not trigger classification response headers
- [ ] Header value contains JSON with highest level, labels, client_override flag
- [ ] Blocked requests (HTTP 451) include classification in response body
- [ ] Classification_Level in every audit log entry (null-safe)
- [ ] Entity labels and client override flag in audit metadata
- [ ] No raw PII in classification audit fields
- [ ] Hypothesis invariants confirmed: deterministic, monotonic, increase-only
- [ ] All test files pass (unit + property + integration)
- [ ] All files committed to git
</success_criteria>
