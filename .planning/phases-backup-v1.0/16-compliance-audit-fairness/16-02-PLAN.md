---
phase: 16-compliance-audit-fairness
plan: 02
type: execute
wave: 2
depends_on: [14, 16-01]
files_modified:
  - src/anonreq/lineage/__init__.py
  - src/anonreq/lineage/tracker.py
  - src/anonreq/lineage/archive.py
  - src/anonreq/models/lineage.py
  - src/anonreq/retention/__init__.py
  - src/anonreq/retention/tiers.py
  - src/anonreq/retention/legal_hold.py
  - src/anonreq/governance/supplier.py
  - src/anonreq/governance/router.py
  - tests/test_data_lineage.py
  - tests/test_retention_tiers.py
  - tests/test_legal_hold.py
  - tests/test_supplier_governance.py
autonomous: true
requirements:
  - REQ-33
  - REQ-44
  - REQ-45
  - REQ-46

user_setup: []

must_haves:
  truths:
    - "Immutable lineage records per session with full provenance — no API to modify or delete"
    - "Lineage stored in PostgreSQL (queryable) + MinIO archive (per-session JSONL)"
    - "Retention tiers: PostgreSQL 90 days, MinIO WORM 7 years, Valkey TTL, Legal Hold infinite"
    - "Legal Hold: tenant-level + record-level tagging, blocks deletion across all storage tiers"
    - "Supplier governance: provider inventory with contract/risk/review status using Phase 14 lifecycle"
    - "Provider review cycle: default 365 days, overdue reviews in governance status"
  artifacts:
    - path: "src/anonreq/lineage/tracker.py"
      provides: "Immutable data lineage recording per session"
      exports: ["LineageTracker", "record_lineage", "query_lineage"]
      min_lines: 100
    - path: "src/anonreq/lineage/archive.py"
      provides: "Lineage archival to MinIO for long-term compliance"
      exports: ["archive_lineage", "get_archived_lineage"]
      min_lines: 60
    - path: "src/anonreq/retention/tiers.py"
      provides: "Retention tier management with configurable schedules"
      exports: ["RetentionManager", "get_retention_config", "purge_expired"]
      min_lines: 80
    - path: "src/anonreq/retention/legal_hold.py"
      provides: "Legal Hold with tenant-level and record-level tagging"
      exports: ["LegalHoldManager", "activate_hold", "release_hold", "is_on_hold"]
      min_lines: 80
    - path: "src/anonreq/governance/supplier.py"
      provides: "Third-party AI supplier governance with Phase 14 lifecycle"
      exports: ["SupplierGovernance", "create_supplier", "get_supplier", "list_overdue_reviews"]
      min_lines: 80
  key_links:
    - from: "src/anonreq/lineage/tracker.py"
      to: "src/anonreq/lineage/archive.py"
      via: "LineageTracker.record_lineage calls archive_lineage after PostgreSQL write"
      pattern: "archive_lineage"
    - from: "src/anonreq/retention/legal_hold.py"
      to: "src/anonreq/retention/tiers.py"
      via: "Legal hold check blocks purge_expired from deleting records on hold"
      pattern: "purge_expired"
    - from: "src/anonreq/governance/supplier.py"
      to: "src/anonreq/governance/router.py"
      via: "Supplier governance endpoints added to governance router"
      pattern: "supplier"
---

<objective>
Build immutable data lineage, retention management, Legal Hold, and supplier governance for Phase 16. This wave delivers: (1) immutable per-session lineage records with PostgreSQL + MinIO archive per D-009/D-010/D-011, (2) retention tier management per D-017, (3) Legal Hold with tenant-level + record-level tagging per D-018/D-019/D-020, and (4) third-party AI supplier governance per D-012 through D-016.

Purpose: Data lineage proves what happened to every request. Retention schedules ensure compliance. Legal Hold prevents deletion during litigation. Supplier governance manages third-party AI relationships.

Output: Every session has immutable lineage record. Retention schedules enforced. Legal Hold blocks deletion. Supplier reviews tracked.
</objective>

<execution_context>
@/Users/aljaunia/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/aljaunia/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/16-compliance-audit-fairness/16-CONTEXT.md
@.planning/phases/16-compliance-audit-fairness/16-ARCHITECTURE.md
@.planning/phases/16-compliance-audit-fairness/16-DISCUSSION-LOG.md
@.planning/phases/16-compliance-audit-fairness/16-SECURITY-ACCEPTANCE.md
@.planning/phases/14-ai-governance-oversight/14-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement immutable data lineage (PostgreSQL + MinIO archive)</name>
  <files>
    src/anonreq/lineage/__init__.py,
    src/anonreq/lineage/tracker.py,
    src/anonreq/lineage/archive.py,
    src/anonreq/models/lineage.py,
    tests/test_data_lineage.py
  </files>
  <behavior>
    - Test 1: record_lineage writes lineage record to PostgreSQL
    - Test 2: record_lineage also archives JSONL to MinIO
    - Test 3: query_lineage retrieves by session_id, tenant_id, provider, model, date range
    - Test 4: No API to modify or delete lineage records (verify no update/delete endpoints)
    - Test 5: Lineage record includes: session_id, tenant_id, timestamps, provider, model, entities, policies
    - Test 6: Archived lineage retrievable from MinIO
  </behavior>
  <action>
    Implement immutable data lineage per D-009, D-010, D-011.

    1. **src/anonreq/models/lineage.py**:
       - `LineageRecord(BaseModel)`: id (str), session_id (str), tenant_id (str), provider (str | None), model (str | None), entity_types (list[str]), entity_count (int), policies_applied (list[str]), classification_action (str | None), processing_time_ms (int), request_timestamp (datetime), response_timestamp (datetime | None), cache_hit (bool=False), success (bool=True), error_type (str | None)

    2. **src/anonreq/lineage/tracker.py**:
       - `class LineageTracker`:
         - `__init__(self, db: AsyncSession, archive_service: "LineageArchiver")`
         - `async def record_lineage(self, record: LineageRecord) -> str`:
           - Inserts into PostgreSQL `data_lineage` table
           - Calls `archive_service.archive_lineage(record)` for MinIO archival
           - Returns record id
         - `async def query_lineage(self, session_id: str | None = None, tenant_id: str | None = None, provider: str | None = None, model: str | None = None, date_from: datetime | None = None, date_to: datetime | None = None, skip: int = 0, limit: int = 50) -> list[LineageRecord]`:
           - Queryable by any combination of filters
           - Returns matching records
         - `async def get_lineage_by_session(self, session_id: str) -> LineageRecord | None`
         - NO update or delete methods — intentional per D-011

    3. **src/anonreq/lineage/archive.py**:
       - `class LineageArchiver`:
         - `__init__(self, minio_client: Minio, bucket: str = "anonreq-lineage-archive")`:
           - MinIO bucket for lineage archive
         - `async def ensure_bucket(self) -> bool`
         - `async def archive_lineage(self, record: LineageRecord) -> str`:
           - Appends JSONL line to `lineage/{year}/{month}/{tenant_id}.jsonl`
           - Or creates per-session file: `lineage/{year}/{month}/{day}/{session_id}.json`
           - Returns MinIO object path
         - `async def get_archived_lineage(self, path: str) -> dict | None`
         - `async def query_archive(self, tenant_id: str, date_from: datetime, date_to: datetime) -> list[dict]`:
           - Lists and concatenates JSONL files for the date range

    4. **Wire into pipeline**:
       - After pipeline completes (success or failure), call `record_lineage` with processing context data
       - Integration point in pipeline orchestration (Phase 2 pipeline completion hook)

    5. **tests/test_data_lineage.py**:
       - Test lineage recording
       - Test PostgreSQL + MinIO dual storage
       - Test query with filters
       - Test no update/delete capability
       - Test archived lineage retrieval
  </action>
  <verify>
    <automated>
      python3 -c "
      from anonreq.models.lineage import LineageRecord
      rec = LineageRecord(session_id='s1', tenant_id='acme', provider='openai', model='gpt-4', entity_types=['EMAIL'], entity_count=1, policies_applied=['anonymize'], processing_time_ms=150)
      assert rec.entity_count == 1
      assert rec.success == True
      print('Lineage model OK')
      "
    </automated>
  </verify>
  <done>Immutable data lineage records per session with full provenance. PostgreSQL + MinIO dual storage. No modify/delete API. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement retention tier management</name>
  <files>
    src/anonreq/retention/__init__.py,
    src/anonreq/retention/tiers.py,
    tests/test_retention_tiers.py
  </files>
  <behavior>
    - Test 1: Retention config loaded with correct defaults (PostgreSQL 90d, MinIO 7y, Valkey TTL)
    - Test 2: purge_expired deletes records past their retention period
    - Test 3: Records under Legal Hold are excluded from purge
    - Test 4: Retention schedule configurable per tier
    - Test 5: purge_expired logs count of deleted records
  </behavior>
  <action>
    Implement retention tier management per D-017.

    1. **src/anonreq/retention/tiers.py**:
       - `RETENTION_TIERS = {
           "postgresql": {"duration_days": 90, "description": "Operational queries"},
           "minio_worm": {"duration_days": 2555, "description": "Compliance archive (7 years)"},
           "valkey": {"duration_days": None, "description": "TTL-based (ephemeral)"},
           "legal_hold": {"duration_days": None, "description": "Infinite until release"},
       }`
       - `class RetentionManager`:
         - `__init__(self, db: AsyncSession, legal_hold_manager, config: dict | None = None)`
         - `async def get_retention_config(self) -> dict`: returns current retention configuration
         - `async def update_retention_config(self, tier: str, duration_days: int) -> dict`: updates tier duration
         - `async def purge_expired(self, tier: str, dry_run: bool = False) -> dict`:
           - For PostgreSQL tier: deletes lineage records older than `duration_days`, excluding records under Legal Hold
           - For MinIO: removes archived lineage files past retention (only if not under Legal Hold)
           - For Valkey: no-op (TTL handles expiry)
           - Returns `{"tier": tier, "deleted_count": N, "skipped_legal_hold": N, "dry_run": bool}`
         - `async def run_scheduled_purge(self) -> dict`:
           - Runs purge_expired for all applicable tiers
           - Intended to be called by a scheduled task (Phase 20+)

    2. **tests/test_retention_tiers.py**:
       - Test config loading and updating
       - Test purge logic for PostgreSQL records past retention
       - Test Legal Hold exclusion from purge
       - Test dry_run mode (no actual deletion)
       - Test Valkey tier no-op
  </action>
  <verify>
    <automated>
      python3 -c "
      from anonreq.retention.tiers import RETENTION_TIERS
      assert 'postgresql' in RETENTION_TIERS
      assert RETENTION_TIERS['postgresql']['duration_days'] == 90
      assert RETENTION_TIERS['minio_worm']['duration_days'] == 2555
      print('Retention tiers OK')
      "
    </automated>
  </verify>
  <done>Retention tier management with configurable schedules. PostgreSQL 90d, MinIO 7y, Valkey TTL, Legal Hold infinite. Purge respects Legal Hold exclusion. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Legal Hold + supplier governance</name>
  <files>
    src/anonreq/retention/legal_hold.py,
    src/anonreq/governance/supplier.py,
    src/anonreq/governance/router.py,
    src/anonreq/models/lineage.py,
    tests/test_legal_hold.py,
    tests/test_supplier_governance.py
  </files>
  <behavior>
    - Test 1: activate_hold creates tenant-level hold with reason and expires_at
    - Test 2: release_hold sets released_at and returns hold record
    - Test 3: is_on_hold returns True for tenant under active hold
    - Test 4: is_on_hold returns True for specific record tagged with hold
    - Test 5: Hold blocks purge_expired from deleting records
    - Test 6: Supplier record created with contract/risk/review status
    - Test 7: Supplier review cycle defaults to 365 days
    - Test 8: Overdue supplier reviews surfaced in governance status
    - Test 9: Risk re-evaluation triggers (model change, ToS, data residency, AI Act, security incident)
  </behavior>
  <action>
    Implement Legal Hold and supplier governance per D-012 through D-020.

    1. **src/anonreq/models/lineage.py** (add):
       - `LegalHoldRecord(BaseModel)`: id (str), tenant_id (str), scope (Literal["tenant", "record"]), record_id (str | None), reason (str), activated_by (str), activated_at (datetime), expires_at (datetime | None), released_at (datetime | None), released_by (str | None)
       - `SupplierGovernanceRecord(BaseModel)`: id (str), name (str), provider_type (str), contract_status (str), risk_status (str), review_cycle_days (int=365), last_review_date (datetime | None), next_review_date (datetime | None), lifecycle_object_id (str), risk_re_evaluation_triggers (list[str]), created_at (datetime), updated_at (datetime)

    2. **src/anonreq/retention/legal_hold.py**:
       - `class LegalHoldManager`:
         - `__init__(self, db: AsyncSession)`
         - `async def activate_hold(self, tenant_id: str, reason: str, activated_by: str, scope: str = "tenant", record_id: str | None = None, expires_at: datetime | None = None) -> LegalHoldRecord`:
           - Creates hold record
           - If scope="tenant": marks tenant as under hold
           - If scope="record": marks specific record as under hold
           - Emits `legal_hold_activated` audit event
         - `async def release_hold(self, hold_id: str, released_by: str) -> LegalHoldRecord`:
           - Sets released_at and released_by
           - Emits `legal_hold_released` audit event
         - `async def is_on_hold(self, tenant_id: str, record_id: str | None = None) -> bool`:
           - Returns True if tenant has active hold (released_at IS NULL)
           - If record_id specified, also checks record-specific hold
         - `async def list_active_holds(self, tenant_id: str | None = None) -> list[LegalHoldRecord]`
         - `async def get_records_on_hold(self, tenant_id: str) -> list[str]`: returns record_ids under hold

    3. **src/anonreq/governance/supplier.py**:
       - `SUPPLIER_REVIEW_TRIGGERS = ["model_change", "tos_change", "data_residency_change", "ai_act_reclassification", "security_incident"]`
       - `class SupplierGovernance`:
         - `__init__(self, db: AsyncSession, lifecycle_manager)`
         - `async def create_supplier(self, name: str, provider_type: str, contract_status: str, risk_status: str) -> SupplierGovernanceRecord`:
           - Creates with Phase 14 lifecycle (DRAFT stage)
           - Default review_cycle_days = 365
           - Sets next_review_date based on cycle
         - `async def get_supplier(self, supplier_id: str) -> SupplierGovernanceRecord | None`
         - `async def list_suppliers(self, risk_status: str | None = None, contract_status: str | None = None) -> list[SupplierGovernanceRecord]`
         - `async def get_overdue_reviews(self) -> list[SupplierGovernanceRecord]`: next_review_date < now()
         - `async def trigger_risk_re_evaluation(self, supplier_id: str, trigger: str)`:
           - Validates trigger in SUPPLIER_REVIEW_TRIGGERS per D-015
           - Updates risk_status to "re_evaluation_required"
           - Sends notification
         - `async def complete_review(self, supplier_id: str, risk_status: str) -> SupplierGovernanceRecord`:
           - Updates last_review_date, next_review_date, risk_status

    4. **Add to governance router**:
       - Legal Hold: `GET /v1/admin/legal-holds`, `POST /v1/admin/legal-holds`, `POST /v1/admin/legal-holds/{id}/release`
       - Supplier: `GET /v1/admin/suppliers`, `POST /v1/admin/suppliers`, `GET /v1/admin/suppliers/overdue-reviews`, `POST /v1/admin/suppliers/{id}/re-evaluate`
       - Governance status updated to include legal hold count and overdue supplier reviews

    5. **tests**:
       - `tests/test_legal_hold.py`: activate, release, hold check, purge exclusion
       - `tests/test_supplier_governance.py`: CRUD, overdue detection, risk re-evaluation triggers
  </action>
  <verify>
    <automated>
      python3 -c "
      from anonreq.models.lineage import LegalHoldRecord
      hold = LegalHoldRecord(tenant_id='acme', scope='tenant', reason='Litigation case #123', activated_by='legal-officer')
      assert hold.scope == 'tenant'
      assert hold.released_at is None
      print('Legal Hold model OK')
      "
    </automated>
  </verify>
  <done>Legal Hold blocks deletion across all storage tiers. Supplier governance with Phase 14 lifecycle and 365-day review cycle. Risk re-evaluation triggers configurable. All tests pass.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pipeline → Lineage tracker | Session metadata recorded per request |
| Retention purge → PostgreSQL/MinIO | Data deleted based on retention schedule |
| Legal Hold API → Storage | Hold state checked before deletion |
| Supplier governance → Phase 14 lifecycle | Supplier state managed through governance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02-01 | Tampering | Data lineage records | mitigate | No API to modify or delete — append-only enforced at DB level |
| T-16-02-02 | Tampering | Retention purge bypass | mitigate | Legal Hold blocks deletion; purge respects hold status via is_on_hold |
| T-16-02-03 | Denial of Service | Legal Hold | mitigate | Auth-protected; only authorized legal/compliance roles can activate |
| T-16-02-04 | Tampering | Supplier review bypass | mitigate | Risk re-evaluation requires valid trigger; overdue reviews surfaced in status |

</threat_model>

<verification>
1. `pytest tests/test_data_lineage.py -x --tb=short -v` passes
2. `pytest tests/test_retention_tiers.py -x --tb=short -v` passes
3. `pytest tests/test_legal_hold.py -x --tb=short -v` passes
4. `pytest tests/test_supplier_governance.py -x --tb=short -v` passes
5. Lineage immutable — no update/delete endpoint exists
6. Legal Hold blocks retention purge (integration test)
7. Supplier overdue reviews visible in governance status
</verification>

<success_criteria>
- [ ] Immutable lineage records per session with full provenance
- [ ] PostgreSQL + MinIO dual storage for lineage
- [ ] No update/delete API for lineage records
- [ ] Queryable by session_id, tenant_id, provider, model, date range
- [ ] Retention tiers: PostgreSQL 90d, MinIO 7y, Valkey TTL, Legal Hold infinite
- [ ] purge_expired respects Legal Hold (skips records on hold)
- [ ] Legal Hold: tenant-level + record-level tagging
- [ ] Legal Hold blocks deletion across all storage tiers
- [ ] Supplier governance with Phase 14 lifecycle (DRAFT→APPROVED→PRODUCTION)
- [ ] Supplier review cycle: default 365 days
- [ ] 5 risk re-evaluation triggers: model_change, tos_change, data_residency_change, ai_act_reclassification, security_incident
- [ ] Overdue supplier reviews in governance status
- [ ] All tests pass
</success_criteria>
