---
phase: 10-ai-security-firewall
plan: 04
type: execute
wave: 4
depends_on:
  - 10-01
  - 10-02
  - 10-03
files_modified:
  - src/anonreq/firewall/audit.py
  - src/anonreq/firewall/metrics.py
  - tests/firewall/test_property.py
  - tests/firewall/test_security.py
  - tests/firewall/test_acceptance.py
autonomous: true
requirements:
  - FIREWALL-05
  - FIREWALL-06

user_setup: []

must_haves:
  truths:
    - "anonreq_prompt_security_events_total counter is labeled by event_type, tenant_id, category"
    - "Structured audit entries contain event_type, session_id, tenant_id, confidence, rule_id — no raw content"
    - "Property tests prove: known injection prompts detected, benign prompts not blocked"
    - "Property tests prove: streaming detection catches cross-chunk injection"
    - "Property tests prove: audit events never contain raw prompt/response content"
  artifacts:
    - path: "src/anonreq/firewall/audit.py"
      provides: "Firewall audit event types and publisher"
      exports: ["FirewallAuditPublisher", "FirewallAuditEvent"]
      min_lines: 60
    - path: "src/anonreq/firewall/metrics.py"
      provides: "Prometheus counters for firewall events"
      exports: ["FirewallMetrics", "register_firewall_metrics"]
      min_lines: 40
    - path: "tests/firewall/test_property.py"
      provides: "Hypothesis property tests for firewall invariants"
      contains: "test_"
      min_lines: 80
    - path: "tests/firewall/test_security.py"
      provides: "Security acceptance tests for all 7 categories + no-PII in logs"
      contains: "test_"
      min_lines: 80
  key_links:
    - from: "src/anonreq/firewall/audit.py"
      to: "src/anonreq/audit/logger.py"
      via: "FirewallAuditPublisher uses shared audit logger"
      pattern: "AuditLogger"
    - from: "src/anonreq/firewall/metrics.py"
      to: "src/anonreq/metrics/registry.py"
      via: "FirewallMetrics registers with global Prometheus registry"
      pattern: "prometheus_client"
    - from: "tests/firewall/test_property.py"
      to: "src/anonreq/firewall/engine.py"
      via: "Property tests exercise rule engine with generated inputs"
      pattern: "FirewallRuleEngine"
---

<objective>
Complete the Phase 10 test suite with audit, metrics, property-based tests, and security acceptance gates. This plan delivers: (1) FirewallAuditPublisher — structured audit events (firewall_injection_detected, firewall_outbound_violation, firewall_rule_reloaded); (2) FirewallMetrics — anonreq_prompt_security_events_total counter with event_type, tenant_id, category labels; (3) Hypothesis property tests proving detection invariants; (4) Integration tests for all 4 gates; (5) Security acceptance tests confirming all 7 categories detectable and no PII in audit events.

Purpose: Without audit, metrics, property tests, and security gates, Phase 10 cannot be verified for correctness, observed in production, or released through security review.
</objective>

<execution_context>
@/Users/aljaunia/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/aljaunia/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/10-ai-security-firewall/10-SECURITY-ACCEPTANCE.md
@.planning/phases/10-ai-security-firewall/10-TEST-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement FirewallAuditPublisher and FirewallMetrics</name>
  <files>
    src/anonreq/firewall/audit.py,
    src/anonreq/firewall/metrics.py,
    tests/firewall/test_audit_metrics.py
  </files>
  <behavior>
    Audit:
    - Test 1: firewall_injection_detected event contains event_type, session_id, tenant_id, confidence, rule_id, category
    - Test 2: firewall_outbound_violation event contains event_type, severity, action
    - Test 3: firewall_rule_reloaded event contains rule_count, version, timestamp
    - Test 4: No audit event contains raw prompt/response content
    - Test 5: matched_text_snippet is max 50 chars (context only, not full value)
    Metrics:
    - Test 6: anonreq_prompt_security_events_total registered with event_type label
    - Test 7: Counter increments on injection detection
    - Test 8: Counter increments on outbound violation
    - Test 9: Labels: event_type, tenant_id, category — all bounded
    - Test 10: Counter values accessible via /metrics endpoint
  </behavior>
  <action>
    Implement firewall audit publisher and metrics.

    1. **src/anonreq/firewall/audit.py**:
       - `FirewallAuditEvent(str, Enum)`: `INJECTION_DETECTED = "firewall_injection_detected"`, `OUTBOUND_VIOLATION = "firewall_outbound_violation"`, `RULE_RELOADED = "firewall_rule_reloaded"`
       - `FirewallAuditPublisher` class:
         - Constructor: `__init__(self, audit_logger)`
         - `async def publish_injection(self, result: DetectionResult, ctx: ProcessingContext) -> None`:
           - Event type: firewall_injection_detected
           - Fields: event_type, timestamp, tenant_id, session_id, category, confidence, rule_id, severity, action
           - `matched_text_snippet`: first 50 chars of match context only
         - `async def publish_outbound_violation(self, result: DetectionResult, ctx: ProcessingContext) -> None`:
           - Event type: firewall_outbound_violation
           - Same field set
         - `async def publish_rule_reloaded(self, old_count: int, new_count: int, version: str) -> None`:
           - Event type: firewall_rule_reloaded
           - Fields: old_rule_count, new_rule_count, version, timestamp
         - `def _truncate_snippet(self, text: str, max_chars: int = 50) -> str`:
           - Truncate to max_chars, adding "..." if needed

    2. **src/anonreq/firewall/metrics.py**:
       - `FirewallMetrics` class (singleton):
         - Register: `Counter("anonreq_prompt_security_events_total", "Prompt security events", ["event_type", "tenant_id", "category"])`
         - `record_injection(tenant_id: str, category: str)`
         - `record_outbound_violation(tenant_id: str, category: str)`
         - `record_rule_reload()`
         - Register all metrics at import time (not dynamically)

    3. **tests/firewall/test_audit_metrics.py**:
       - Test each audit event type
       - Test snippet truncation
       - Test no raw content in any event
       - Test metric registration and increment
       - Test metric labels
  </action>
  <verify>
    <automated>
      ANONREQ_API_KEY=testkey1234567890123456789012345678 \
      ANONREQ_VALKEY_URL=redis://localhost:6379 ANONREQ_PRESIDIO_URL=http://localhost:5001 \
      pytest tests/firewall/test_audit_metrics.py -x --tb=short -v 2>&1 | tail -20
    </automated>
  </verify>
  <done>All 3 required audit events implemented: firewall_injection_detected, firewall_outbound_violation, firewall_rule_reloaded. No raw content in events. matched_text_snippet truncated to 50 chars. Prometheus counter anonreq_prompt_security_events_total registered with bounded labels. All audit/metrics tests pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement property-based tests with Hypothesis</name>
  <files>
    tests/firewall/test_property.py
  </files>
  <action>
    Implement Hypothesis property-based tests for firewall invariants.

    1. **tests/firewall/test_property.py**:
       - Import Hypothesis: `from hypothesis import given, assume, strategies as st, settings`
       - Define strategies:
         - `known_injection_text`: generates text containing known injection patterns
         - `benign_text`: generates text without injection patterns (random sentences)
         - `chunk_sequence`: generates list of string chunks of varying sizes
         - `split_position`: generates integer positions at which to split text

       - Property tests:
         - `test_known_injection_always_detected(known_injection_text)`:
           - All known injection patterns are detected above configurable threshold
         - `test_benign_prompt_not_blocked(benign_text)`:
           - Benign prompts never produce BLOCK action
           - False positive rate at or below configured threshold
         - `test_streaming_catches_cross_chunk_injection(injection_text, split_position)`:
           - For any injection text split at any position across two chunks
           - Sliding window detection catches the cross-chunk pattern
         - `test_audit_events_no_raw_content(injection_text)`:
           - For any detected injection, audit events never contain raw prompt/response content
           - Verify snippet is truncated and non-revealing
         - `test_seven_categories_all_detectable()`:
           - Generate known patterns for each of the 7 detection categories
           - Verify each category's detection works independently

    2. Run with `pytest tests/firewall/test_property.py -x --tb=short -v --hypothesis-show-statistics`
  </action>
  <verify>
    <automated>
      ANONREQ_API_KEY=testkey1234567890123456789012345678 \
      ANONREQ_VALKEY_URL=redis://localhost:6379 ANONREQ_PRESIDIO_URL=http://localhost:5001 \
      pytest tests/firewall/test_property.py -x --tb=short -v 2>&1 | tail -30
    </automated>
  </verify>
  <done>Hypothesis property tests prove: known injection always detected, benign prompts never blocked, streaming detection catches cross-chunk injection at every split position, audit events never contain raw content, all 7 detection categories functional. All property tests pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Security acceptance tests + release gate verification</name>
  <files>
    tests/firewall/test_security.py,
    tests/firewall/test_acceptance.py
  </files>
  <action>
    Implement security acceptance and release gate tests.

    1. **tests/firewall/test_security.py**:
       - `test_injection_7_categories_all_detectable`:
         - For each of the 7 categories: send known pattern, verify detected
       - `test_inbound_pre_anon_catches_raw_injection`:
         - Pre-anon gate catches injection in raw input
       - `test_inbound_post_anon_catches_sanitized_injection`:
         - Post-anon gate catches injection surviving sanitization
       - `test_outbound_inspection_blocks_violations`:
         - Outbound gate blocks policy-violating content with 451
       - `test_no_pii_in_firewall_audit_events`:
         - Generate events with synthetic PII-containing prompts
         - Capture audit output, verify no PII substrings
         - Verify matched_text_snippet is truncated and non-revealing
       - `test_latency_budgets_respected`:
         - Rules-only: evaluation completes within 50ms
         - Rules + ML: evaluation completes within 200ms (or skipped)
       - `test_hot_reload_within_60s`:
         - Modify rule file, verify reload within 60s
         - Verify no events dropped during reload

    2. **tests/firewall/test_acceptance.py** (release gate):
       - `test_required_detection_categories_present`:
         - Verify all 7 categories in SECURITY-ACCEPTANCE.md are detectable
       - `test_required_audit_events_present`:
         - Verify firewall_injection_detected, firewall_outbound_violation, firewall_rule_reloaded emitted
       - `test_required_metrics_present`:
         - Scrape /metrics, assert anonreq_prompt_security_events_total present
       - `test_zero_false_blocks_on_benign_corpus`:
         - Run benign test corpus, verify zero BLOCK actions
       - `test_security_acceptance_gates_pass`:
         - Verify all criteria in 10-SECURITY-ACCEPTANCE.md pass
  </action>
  <verify>
    <automated>
      ANONREQ_API_KEY=testkey1234567890123456789012345678 \
      ANONREQ_VALKEY_URL=redis://localhost:6379 ANONREQ_PRESIDIO_URL=http://localhost:5001 \
      pytest tests/firewall/test_security.py tests/firewall/test_acceptance.py -x --tb=short -v 2>&1 | tail -30
    </automated>
  </verify>
  <done>Security acceptance tests confirm all 7 detection categories functional, dual-gate inbound/outbound detection works, zero PII in audit events, latency budgets respected, hot-reload within 60s. Release gate tests verify all required audit events, metrics, and security acceptance criteria pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Detection result → Audit event | Confidence scores and categories enter structured log |
| Metric labels → Prometheus | Bounded label sets exported via /metrics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-TC1 | Information Disclosure | matched_text_snippet | mitigate | Snippet truncated to 50 chars; contains match context only — never full prompt/response |
| T-10-04-TC2 | Tampering | Metric label injection | mitigate | Bounded label values (event_type, tenant_id, category); no user-controlled labels |
| T-10-04-TC3 | Information Disclosure | Property test data | accept | All test data is synthetic; no real PII in test suite |
</threat_model>

<verification>
1. `pytest tests/firewall/test_audit_metrics.py -x --tb=short -v` passes
2. `pytest tests/firewall/test_property.py -x --tb=short -v` passes with Hypothesis statistics
3. `pytest tests/firewall/test_security.py -x --tb=short -v` passes
4. `pytest tests/firewall/test_acceptance.py -x --tb=short -v` passes
5. Full test suite: `pytest tests/firewall/ -x --tb=short` passes
6. All 7 detection categories confirmed functional
7. No PII in any audit event output (automated scan)
8. Latency budgets respected: rules < 50ms, rules+ML < 200ms
9. Hot-reload completes within 60s without dropped events
</verification>

<success_criteria>
- [ ] FirewallAuditPublisher: 3 event types (injection_detected, outbound_violation, rule_reloaded)
- [ ] No raw prompt/response content in any audit event
- [ ] matched_text_snippet truncated to max 50 chars
- [ ] anonreq_prompt_security_events_total with event_type, tenant_id, category labels
- [ ] All label values bounded (no dynamic/user-controlled labels)
- [ ] Property test: known injection always detected
- [ ] Property test: benign prompts not blocked (zero false blocks on corpus)
- [ ] Property test: streaming cross-chunk detection at all split positions
- [ ] Property test: audit events never contain raw content
- [ ] Security test: all 7 categories detectable in integration tests
- [ ] Security test: latency budgets respected (50ms/200ms)
- [ ] Security test: hot-reload within 60s without dropped events
- [ ] Release gate: all SECURITY-ACCEPTANCE.md criteria pass
- [ ] Full `pytest tests/firewall/ -x --tb=short` passes
- [ ] All files committed to git
