---
phase: 30-enterprise-authentication-rbac
plan: 02
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - src/anonreq/middleware/rbac.py
  - src/anonreq/admin/router.py
  - src/anonreq/admin/policy_routes.py
  - src/anonreq/admin/usage_routes.py
  - src/anonreq/admin/compliance_routes.py
  - src/anonreq/admin/incident_routes.py
  - src/anonreq/admin/provider_routes.py
  - tests/admin/test_rbac.py
  - tests/unit/admin/test_role_normalization.py
  - tests/integration/test_admin_role_enforcement.py
requirements:
  - SSO-02
---

<objective>
Normalize the RBAC vocabulary to the enterprise role set and apply it consistently across admin and gateway routes so the identity layer can enforce the correct privileges everywhere.

Purpose: the code already has reusable role-based dependencies, but the live role names and route declarations are still carrying the earlier `read_only` vocabulary. This wave makes the role model explicit and consistent.
Output: normalized role constants, updated route decorators, and tests proving the hierarchy and route gates match the enterprise roles.
</objective>

<execution_context>
@/Users/aljaunia/.codex/gsd-core/workflows/execute-plan.md
@/Users/aljaunia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-enterprise-authentication-rbac/30-CONTEXT.md
@.planning/phases/30-enterprise-authentication-rbac/30-RESEARCH.md
@.planning/phases/30-enterprise-authentication-rbac/30-PATTERNS.md
@.planning/phases/30-enterprise-authentication-rbac/30-VALIDATION.md
@src/anonreq/middleware/rbac.py
@src/anonreq/admin/router.py
@src/anonreq/admin/policy_routes.py
@src/anonreq/admin/usage_routes.py
</context>

<phase_scope>
In scope: role name normalization, route-level RBAC updates, hierarchical enforcement, and compatibility handling for existing test seams. Out of scope: JWT verification internals and mTLS ingress validation.
</phase_scope>

<artifacts-this-phase-produces>
- A canonical enterprise role vocabulary.
- Route decorators aligned with the canonical roles.
- Tests proving the hierarchy and route access rules still behave correctly after the rename.
</artifacts-this-phase-produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize RBAC roles and propagate them through admin routes</name>
  <files>src/anonreq/middleware/rbac.py, src/anonreq/admin/router.py, src/anonreq/admin/policy_routes.py, src/anonreq/admin/usage_routes.py, src/anonreq/admin/compliance_routes.py, src/anonreq/admin/incident_routes.py, src/anonreq/admin/provider_routes.py, tests/admin/test_rbac.py, tests/unit/admin/test_role_normalization.py, tests/integration/test_admin_role_enforcement.py</files>
  <read_first>src/anonreq/middleware/rbac.py, src/anonreq/admin/router.py, src/anonreq/admin/policy_routes.py, src/anonreq/admin/usage_routes.py, tests/admin/test_rbac.py, .planning/phases/30-enterprise-authentication-rbac/30-PATTERNS.md</read_first>
  <behavior>
    - Promote `read_only_auditor` as the canonical lowest-privilege enterprise role.
    - Keep the hierarchy stable so the dependency contract still works with `require_role()`.
    - Update admin route decorators so they reference the canonical roles instead of older role strings.
    - Preserve fail-closed behavior for missing, unknown, or underprivileged principals.
  </behavior>
  <action>Refactor the RBAC role enum and related route decorators to use the enterprise role vocabulary consistently. Add compatibility where necessary so earlier tests and injected principals can be migrated without changing the enforcement semantics. Expand tests to prove the new role names map into the same hierarchy, that insufficient roles still return 403, and that route-level dependencies use the normalized role constants.</action>
  <verify><automated>uv run pytest tests/admin/test_rbac.py tests/unit/admin/test_role_normalization.py tests/integration/test_admin_role_enforcement.py -q</automated></verify>
  <acceptance_criteria>
    - "Enterprise roles are the canonical vocabulary."
    - "RBAC dependency checks still enforce the hierarchy correctly."
    - "Admin routes block unauthorized access with HTTP 403."
  </acceptance_criteria>
  <done>`uv run pytest tests/admin/test_rbac.py tests/unit/admin/test_role_normalization.py tests/integration/test_admin_role_enforcement.py -q` passes and the role model is normalized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Principal -> route gate | Route access is trusted only after role normalization and hierarchy checks. |
| Route decorator -> admin action | A route must explicitly declare the minimum role it needs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-30-03 | Elevation of Privilege | RBAC role mapping | mitigate | Normalize role names and preserve strict hierarchy comparisons. |
| T-30-04 | Spoofing | route principal injection | mitigate | Accept only principals created by the verified auth layer or trusted upstream tests. |

</threat_model>

<verification>
1. Run `uv run pytest tests/admin/test_rbac.py tests/unit/admin/test_role_normalization.py tests/integration/test_admin_role_enforcement.py -q`.
2. Confirm the enterprise role names are reflected in the route decorators and request-state principal.
3. Confirm missing or underprivileged principals still receive structured 401/403 responses.
</verification>

<success_criteria>
1. RBAC uses the enterprise role names everywhere.
2. Role enforcement is still hierarchical and fail-closed.
3. Admin routes declare and enforce the correct minimum role.
</success_criteria>

