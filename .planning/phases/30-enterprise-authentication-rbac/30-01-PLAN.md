---
phase: 30-enterprise-authentication-rbac
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - src/anonreq/admin/auth.py
  - src/anonreq/admin/router.py
  - src/anonreq/auth/oidc.py
  - src/anonreq/main.py
  - tests/unit/auth/test_oidc_jwks_cache.py
  - tests/integration/test_oidc_admin_gate.py
requirements:
  - SSO-01
autonomous: false
---

<objective>
Replace the static admin API-key authentication path with OIDC JWT verification backed by cached JWKS, then project validated claims into the existing principal shape so downstream RBAC code can keep using `request.state.role_principal`.

Purpose: phase 30 needs a real identity boundary before any role cleanup can be trusted. The auth layer must verify tokens once, cache keys locally, and fail closed on invalid or expired claims.
Output: an OIDC auth service, local JWKS cache, startup wiring, and tests proving admin requests succeed only with valid identity tokens.
</objective>

<execution_context>
@/Users/aljaunia/.codex/gsd-core/workflows/execute-plan.md
@/Users/aljaunia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-enterprise-authentication-rbac/30-CONTEXT.md
@.planning/phases/30-enterprise-authentication-rbac/30-RESEARCH.md
@.planning/phases/30-enterprise-authentication-rbac/30-PATTERNS.md
@.planning/phases/30-enterprise-authentication-rbac/30-VALIDATION.md
@src/anonreq/admin/auth.py
@src/anonreq/admin/router.py
@src/anonreq/middleware/rbac.py
@tests/admin/test_rbac.py
</context>

<phase_scope>
In scope: OIDC bearer-token verification, JWKS retrieval and local caching, principal projection into request state, and admin route integration. Out of scope: role vocabulary cleanup, mTLS verification, tenant isolation, and unrelated gateway changes.
</phase_scope>

<artifacts-this-phase-produces>
- An OIDC verifier that caches JWKS locally.
- Admin authentication that accepts validated JWTs instead of static API keys.
- Tests proving valid tokens authorize admin access and invalid tokens fail closed.
</artifacts-this-phase-produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add OIDC JWT verification with cached JWKS</name>
  <files>pyproject.toml, uv.lock, src/anonreq/auth/oidc.py, src/anonreq/admin/auth.py, src/anonreq/admin/router.py, src/anonreq/main.py, tests/unit/auth/test_oidc_jwks_cache.py, tests/integration/test_oidc_admin_gate.py</files>
  <read_first>src/anonreq/admin/auth.py, src/anonreq/admin/router.py, src/anonreq/middleware/rbac.py, tests/admin/test_rbac.py, .planning/phases/30-enterprise-authentication-rbac/30-RESEARCH.md</read_first>
  <behavior>
    - Verify JWT signatures against a cached JWKS endpoint before any admin route runs.
    - Validate issuer, audience, expiry, and subject claims with fail-closed errors.
    - Project validated claims into the existing principal shape used by RBAC.
    - Keep token verification metadata-only; do not log token payloads, raw JWKS, or issuer secrets.
  </behavior>
  <action>Build a small OIDC auth module that fetches JWKS once, caches keys locally, and refreshes them on a bounded interval or cache miss. Replace the static admin API-key check with token verification while preserving the current principal injection point so downstream RBAC and tests can reuse the same request-state contract. Add tests that cover a valid token path, invalid signature, expired token, and stale-key refresh behavior.</action>
  <verify><automated>uv run pytest tests/unit/auth/test_oidc_jwks_cache.py tests/integration/test_oidc_admin_gate.py -q</automated></verify>
  <acceptance_criteria>
    - "Admin requests authenticate via OIDC JWT verification rather than a static API key."
    - "JWKS keys are cached locally and refreshed without requiring a restart."
    - "Invalid, expired, or issuer-mismatched tokens fail closed."
  </acceptance_criteria>
  <done>`uv run pytest tests/unit/auth/test_oidc_jwks_cache.py tests/integration/test_oidc_admin_gate.py -q` passes and admin auth is OIDC-backed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IdP/JWKS -> gateway auth | JWT claims become trusted only after signature and claim validation. |
| Auth layer -> principal shape | Valid claims are normalized into the existing request-state principal object. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-30-01 | Spoofing | OIDC auth | mitigate | Validate JWT signatures against cached JWKS and check issuer/audience/expiry. |
| T-30-02 | Information Disclosure | token handling | mitigate | Never log raw token payloads, JWKS bodies, or issuer secrets. |

</threat_model>

<verification>
1. Run `uv run pytest tests/unit/auth/test_oidc_jwks_cache.py tests/integration/test_oidc_admin_gate.py -q`.
2. Confirm invalid tokens are rejected before RBAC runs.
3. Confirm the admin principal is still exposed through `request.state.role_principal` after verification.
</verification>

<success_criteria>
1. Admin requests are authenticated by OIDC JWTs.
2. JWKS caching works without restarting the service.
3. Fail-closed behavior remains intact for invalid identity tokens.
</success_criteria>

