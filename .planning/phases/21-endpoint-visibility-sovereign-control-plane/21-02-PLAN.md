---
phase: 21-endpoint-visibility-sovereign-control-plane
plan: 02
type: execute
wave: 1
---

# Plan 21-02: Windows Endpoint Agent

**Objective**: Port the endpoint agent to Windows — Windows service, MSI installer, WMI integration.

**Depends on**: 21-01 (endpoint base classes)

## Tasks

1. Windows service wrapper: `src/anonreq/endpoint/windows/service.py` — pywin32 service, SCM integration
2. Windows traffic capture: `src/anonreq/endpoint/windows/capture.py` — WinPcap/Npcap, Windows Filtering Platform
3. WMI process enumeration for AI app discovery
4. MSI installer: `scripts/package-endpoint-windows.bat` / WiX config
5. Audit events aligned with 21-01 schema
6. 15+ tests (mocked on non-Windows)
