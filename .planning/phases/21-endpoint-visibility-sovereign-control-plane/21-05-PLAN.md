---
phase: 21-endpoint-visibility-sovereign-control-plane
plan: 05
type: execute
wave: 3
---

# Plan 21-05: Hybrid AI Architecture — Provider Selection Engine

**Objective**: Gateway selects provider per-request combining data sensitivity, classification, jurisdiction, and policy.

**Depends on**: 21-04 (sovereign policy), Phase 12 (classification), Phase 17 (provider adapter)

## Tasks

1. `src/anonreq/endpoint/hybrid/selector.py` — `HybridProviderSelector`
2. Decision inputs: `ClassificationLevel`, `Jurisdiction`, data sensitivity tags, user role
3. Decision matrix: weighted scoring for provider selection
4. Fallback logic: primary → secondary → fail-secure (block)
5. `hybrid_provider_selected`, `hybrid_fallback`, `hybrid_blocked` audit events
6. Prometheus metrics: `hybrid_selections_total` by provider, `hybrid_fallbacks_total`
7. 15+ tests
