---
phase: 23-engineering-hygiene
plan: 03
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - .github/workflows/test.yml
autonomous: true
requirements:
  - HYG-01
user_setup: []

must_haves:
  truths:
    - "Every push to main triggers GitHub Actions running the full pytest suite"
    - "Every PR targeting main triggers GitHub Actions running the full pytest suite"
    - "ruff violations cause CI failure"
    - "mypy violations cause CI failure"
    - "Tests are run with coverage reporting (70% threshold, 60% block)"
    - "Load tests are excluded from auto-CI (manual trigger only)"
    - "uv cache is preserved across CI runs for faster installs"
  artifacts:
    - path: .github/workflows/test.yml
      provides: "CI/CD workflow definition for test, lint, and type-check"
      exports: ["ruff check", "mypy src/", "pytest", "coverage report"]
    - path: pyproject.toml
      provides: "tool config for ruff and mypy read by CI"
      contains: "[tool.ruff]", "[tool.mypy]"  (modified in Plan 01)
  key_links:
    - from: .github/workflows/test.yml
      to: pyproject.toml
      via: "uv sync --group dev reads dev deps; ruff/mypy read tool config"
    - from: .github/workflows/test.yml
      to: tests/
      via: "pytest discovers and runs all matching test files"
    - from: .github/workflows/test.yml
      to: ~/.cache/uv
      via: "actions/cache@v4 preserves uv cache across runs (D-13)"
---

<objective>
Create a GitHub Actions CI/CD workflow that runs ruff check, mypy, the full pytest suite, and coverage reporting on every push/PR to main.

Purpose: Automate quality enforcement for all code changes. Establishes CI foundation required by Phases 24-26.
Output: `.github/workflows/test.yml` (new file).
</objective>

<execution_context>
@/Users/aljaunia/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/aljaunia/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.github/workflows/docs-ci.yml
@.planning/phases/23-engineering-hygiene/CONTEXT.md
@.planning/v1.5-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .github/workflows/test.yml with ruff, mypy, pytest, and coverage steps</name>
  <files>.github/workflows/test.yml</files>
  <read_first>
    .github/workflows/docs-ci.yml — read the existing workflow to match naming/structural conventions (indentation style, step naming, action versions).
  </read_first>
  <action>
    Create `.github/workflows/test.yml` with the following structure:

    ```yaml
    name: Test & Lint

    on:
      push:
        branches: [main]
      pull_request:
        branches: [main]

    jobs:
      test:
        name: Python 3.12
        runs-on: ubuntu-24.04

        steps:
          - uses: actions/checkout@v4

          - name: Install uv
            uses: astral-sh/setup-uv@v5
            with:
              enable-cache: true
              cache-dependency-glob: pyproject.toml

          - name: Install dependencies
            run: uv sync --group dev

          - name: Lint with ruff
            run: uv run ruff check src/ tests/

          - name: Type check with mypy
            run: uv run mypy src/

          - name: Test with pytest
            run: uv run pytest tests/ -x -v --ignore=tests/load -m "not load" --junitxml=junit.xml

          - name: Coverage report
            run: uv run pytest tests/ --cov=anonreq --cov-report=term-missing --cov-report=xml --ignore=tests/load -m "not load"

          - name: Upload coverage to CI summary
            if: always()
            run: |
              echo "## Coverage Summary" >> $GITHUB_STEP_SUMMARY
              uv run python -c "
      import xml.etree.ElementTree as ET
      tree = ET.parse('coverage.xml')
      root = tree.getroot()
      line_rate = float(root.attrib['line-rate']) * 100
      print(f'Line coverage: {line_rate:.1f}%')
      if line_rate < 70:
          print(f'⚠ Warning: Coverage {line_rate:.1f}% is below 70% threshold')
      if line_rate < 60:
          print(f'❌ Error: Coverage {line_rate:.1f}% is below 60% hard minimum')
          exit(1)
      " >> $GITHUB_STEP_SUMMARY
    ```

    Per D-01: GitHub Actions CI.
    Per D-02: Trigger on push/PR to main; load tests excluded.
    Per D-03: Single Python 3.12 matrix.
    Per D-04: `uv sync --group dev` for dependencies.
    Per D-05: No Docker service containers (tests use fakeredis/respx mocks).
    Per D-06/D-08: ruff and mypy run with config from pyproject.toml.
    Per D-10: Coverage 70% soft threshold, 60% hard block.
    Per D-13: Use astral-sh/setup-uv@v5 with enable-cache (uses actions/cache@v4 underneath). This is the standard uv CI caching approach and makes explicit actions/cache steps unnecessary.
    Per D-14: Run `--ignore=tests/load -m "not load"`.
  </action>
  <verify>
    <automated>python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/test.yml')); assert 'on' in d; assert 'push' in d['on']; assert 'pull_request' in d['on']; assert 'test' in d['jobs']; print('Workflow structure valid')"</automated>
  </verify>
  <done>
    .github/workflows/test.yml exists, valid YAML, triggers on push/PR to main, contains jobs with ruff check, mypy, pytest, and coverage reporting steps.
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate workflow structure and simulate step verification</name>
  <files>.github/workflows/test.yml</files>
  <read_first>
    .github/workflows/test.yml — re-read the created file to verify completeness.
  </read_first>
  <action>
    Run a series of validation checks on the workflow file:

    1. Parse YAML and verify required jobs exist:
       - Job "test" exists
       - Step "Lint with ruff" runs `uv run ruff check src/ tests/`
       - Step "Type check with mypy" runs `uv run mypy src/`
       - Step "Test with pytest" runs pytest with `-m "not load"` and `--ignore=tests/load`
       - Step "Coverage report" exists

    2. Verify the workflow lint-and-test commands are compatible with pyproject.toml:
       - `ruff check src/ tests/` — verify the ruff config exists in pyproject.toml
       - `mypy src/` — verify the mypy config exists in pyproject.toml

    3. If any issues found, fix them in the YAML file.
  </action>
  <verify>
    <automated>
      python3 -c "
import yaml
with open('.github/workflows/test.yml') as f:
    d = yaml.safe_load(f)
steps = d['jobs']['test']['steps']
step_names = [s.get('name', '') for s in steps]
step_runs = [s.get('run', '') for s in steps if 'run' in s]
assert 'Lint with ruff' in step_names, 'Missing ruff step'
assert 'Type check with mypy' in step_names, 'Missing mypy step'
assert 'Test with pytest' in step_names, 'Missing pytest step'
assert any('ruff check' in r for r in step_runs), 'ruff check command missing'
assert any('mypy src' in r for r in step_runs), 'mypy command missing'
assert any('not load' in r for r in step_runs), 'load test exclusion missing'
print('All validation checks passed')
"
    </automated>
  </verify>
  <done>
    Workflow YAML is valid, all required steps are present, commands match pyproject.toml configuration.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub → external runner | CI workflow executes on GitHub-hosted runners |
| Runner → package registries | uv/pip installs packages from PyPI during CI |
| PR author → workflow definition | Modified workflows run with repository secrets access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-06 | Tampering | CI workflow file | mitigate | Workflow changes reviewed as part of PR; branch protection on main requires passing CI |
| T-23-07 | Tampering | uv piped packages (supply chain) | accept | astral-sh/setup-uv@v5 pins uv version; `uv sync` respects lockfile; [ASSUMED] for well-known packages |
| T-23-08 | Elevation of Privilege | PR fork workflows | accept | GitHub treats PRs from forks as untrusted by default; no secrets exposed to fork PRs |
</threat_model>

<verification>
1. `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/test.yml'))"` — valid YAML
2. `grep -c "uv run ruff check src/ tests/" .github/workflows/test.yml` — ruff check step exists
3. `grep -c "uv run mypy src/" .github/workflows/test.yml` — mypy step exists
4. `grep -c "not load" .github/workflows/test.yml` — load test exclusion present
5. `grep -c "enable-cache: true" .github/workflows/test.yml` — uv caching enabled
</verification>

<success_criteria>
- `.github/workflows/test.yml` created per SPEC §1.1 and D-01 through D-14
- Workflow triggers on `push` to `main` and `pull_request` targeting `main` (D-02)
- Single Python 3.12 matrix (D-03)
- Steps: `uv sync --group dev` → `ruff check` → `mypy src/` → `pytest` → coverage (D-04, D-06, D-08, D-10)
- Load tests excluded via `-m "not load"` and `--ignore=tests/load` (D-14)
- uv caching enabled via astral-sh/setup-uv@v5 (D-13)
- No Docker service containers (D-05)
- Developers can run identical commands locally: `uv run ruff check src/ tests/`, `uv run mypy src/`, `uv run pytest tests/ -x -v -m "not load"`
</success_criteria>

<output>
Create `.planning/phases/23-engineering-hygiene/23-03-SUMMARY.md` when done
</output>
