---
phase: 23-engineering-hygiene
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
autonomous: true
requirements:
  - HYG-03
user_setup: []

must_haves:
  truths:
    - "`docker compose up` exposes only gateway port 8080 on the host"
    - "Grafana does not allow anonymous access (GF_AUTH_ANONYMOUS_ENABLED=false)"
    - "Observability services (postgres, minio, prometheus, grafana) are only reachable via internal Docker network"
    - "Production TLS termination guidance is documented in docker-compose.yml"
  artifacts:
    - path: docker-compose.yml
      provides: "Secure Docker Compose orchestration with no unnecessary host port exposure"
      contains: "no host ports on postgres/minio/prometheus/grafana; GF_AUTH_ANONYMOUS_ENABLED=false; TLS comment"
  key_links:
    - from: docker-compose.yml
      to: "host network"
      via: "only ports: [8080:8080] on anonreq service (per D-12)"
    - from: docker-compose.yml grafana environment
      to: "GF_AUTH_ANONYMOUS_ENABLED=false"
      via: "disables anonymous access to Grafana dashboards"
---

<objective>
Remove host port bindings from all non-gateway services and disable Grafana anonymous authentication, ensuring the default `docker compose up` exposes only port 8080.

Purpose: Eliminate unnecessary attack surface from infrastructure services. Per D-12.
Output: docker-compose.yml (modified).
</objective>

<execution_context>
@/Users/aljaunia/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/aljaunia/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@docker-compose.yml
@.planning/phases/23-engineering-hygiene/CONTEXT.md
@.planning/v1.5-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove host port bindings from postgres, minio, prometheus, and grafana</name>
  <files>docker-compose.yml</files>
  <read_first>
    docker-compose.yml — re-read lines 118-226 (the four services with ports to remove). Note the exact YAML structure for each ports: block.
  </read_first>
  <action>
    Edit docker-compose.yml to remove host port bindings from four services:

    1. **postgres** (lines 135-136): Remove the entire `ports:` block:
       ```
       ports:
         - "5432:5432"
       ```

    2. **minio** (lines 175-177): Remove the entire `ports:` block:
       ```
       ports:
         - "9000:9000"
         - "9001:9001"
       ```

    3. **prometheus** (lines 195-196): Remove the entire `ports:` block:
       ```
       ports:
         - "9090:9090"
       ```

    4. **grafana** (lines 219-220): Remove the entire `ports:` block:
       ```
       ports:
         - "3000:3000"
       ```

    Per D-12: Keep anonreq gateway port 8080 exposed. Do NOT touch valkey-exporter or postgres-exporter (they already have no host ports). Do NOT touch presidio-analyzer (no host ports).

    After removing each block, verify indentation of surrounding lines is preserved correctly.
  </action>
  <verify>
    <automated>
      python3 -c "
import yaml
with open('docker-compose.yml') as f:
    data = yaml.safe_load(f)
for svc in ['postgres', 'minio', 'prometheus', 'grafana']:
    assert 'ports' not in data['services'][svc] or not data['services'][svc]['ports'], f'{svc} still has host ports'
assert data['services']['anonreq']['ports'] == ['8080:8080'], 'gateway port 8080 must remain'
print('All checks passed')
"
    </automated>
  </verify>
  <done>
    docker compose config shows no host port bindings for postgres, minio, prometheus, or grafana; anonreq gateway remains on 8080.
  </done>
</task>

<task type="auto">
  <name>Task 2: Disable Grafana anonymous auth and add TLS termination comment</name>
  <files>docker-compose.yml</files>
  <read_first>
    docker-compose.yml — re-read the grafana service environment block (lines 211-214) to see the current anonymous auth configuration.
  </read_first>
  <action>
    Edit docker-compose.yml in the grafana service `environment` block:

    1. Change `GF_AUTH_ANONYMOUS_ENABLED=true` to `GF_AUTH_ANONYMOUS_ENABLED=false` (line 212).
    2. Remove the line `- GF_AUTH_ANONYMOUS_ORG_ROLE=Viewer` (line 213 — no longer needed when anonymous auth is disabled).
    3. Add a comment block near the top of the file (after the header comment block, before `services:`) documenting production TLS expectations:
       ```
       # Production deployments should terminate TLS at a reverse proxy
       # (e.g., nginx, Caddy, Cloudflare Tunnel) upstream of the gateway.
       # The gateway itself does not handle TLS termination.
       ```

    Per D-12: Disable GF_AUTH_ANONYMOUS_ENABLED. SPEC §1.3: remove GF_AUTH_ANONYMOUS_ORG_ROLE, add TLS comment.
  </action>
  <verify>
    <automated>
      python3 -c "
import yaml
with open('docker-compose.yml') as f:
    data = yaml.safe_load(f)
grafana_env = data['services']['grafana']['environment']
assert 'GF_AUTH_ANONYMOUS_ENABLED=false' in grafana_env, 'anonymous auth must be disabled'
assert 'GF_AUTH_ANONYMOUS_ORG_ROLE' not in str(grafana_env), 'GF_AUTH_ANONYMOUS_ORG_ROLE must be removed'
print('Grafana anonymous auth check passed')
with open('docker-compose.yml') as f:
    content = f.read()
assert 'reverse proxy' in content.lower() or 'tls' in content.lower(), 'TLS comment not found'
print('TLS comment found')
"
    </automated>
  </verify>
  <done>
    GF_AUTH_ANONYMOUS_ENABLED=false set in grafana environment; GF_AUTH_ANONYMOUS_ORG_ROLE removed; TLS termination comment present in file header.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker host network ↔ internal container network | Exposed ports bypass internal isolation |
| Human operator → docker-compose.yml | Misconfiguration can expose management interfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03 | Information Disclosure | postgres, minio, prometheus, grafana | mitigate | Host port bindings removed per D-12; services only reachable via internal Docker network |
| T-23-04 | Information Disclosure | grafana dashboards | mitigate | GF_AUTH_ANONYMOUS_ENABLED=false per D-12 |
| T-23-05 | Tampering | docker-compose.yml | accept | Config reviewed via PR; observability profile users must opt in with `--profile observability` |
</threat_model>

<verification>
1. `python3 -c "import yaml; d=yaml.safe_load(open('docker-compose.yml')); print('gateway:', d['services']['anonreq']['ports']); print('postgres ports:', d['services']['postgres'].get('ports', 'none'))"` — verify gateway has 8080:8080, postgres has no ports
2. `grep GF_AUTH_ANONYMOUS_ENABLED docker-compose.yml` — shows "false"
3. `grep -c "GF_AUTH_ANONYMOUS_ORG_ROLE" docker-compose.yml` — exit code 1 (not found)
4. `grep -i "reverse proxy\|TLS\|tls termination" docker-compose.yml` — comment present
</verification>

<success_criteria>
- No host port bindings on postgres, minio, prometheus, or grafana (D-12)
- GF_AUTH_ANONYMOUS_ENABLED=false and GF_AUTH_ANONYMOUS_ORG_ROLE removed (D-12)
- TLS termination comment documented in docker-compose.yml (SPEC §1.3)
- Gateway port 8080 remains exposed
- `docker compose up` (no profile) exposes only port 8080
- `docker compose --profile observability up` works for users needing observability
</success_criteria>

<output>
Create `.planning/phases/23-engineering-hygiene/23-02-SUMMARY.md` when done
</output>
