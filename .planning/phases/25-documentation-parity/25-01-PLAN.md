---
phase: 25-documentation-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/GLOSSARY.md
  - docs/TRANSLATION_MANIFEST.md
  - docs/en/architecture.md
  - docs/en/security.md
  - docs/en/operations.md
  - docs/fr/.gitkeep
  - docs/es/.gitkeep
  - docs/pt/.gitkeep
  - docs/it/.gitkeep
  - docs/ar/.gitkeep
  - docs/ar/README.md
  - docs/nl/.gitkeep
  - docs/en/getting-started.md
  - docs/en/installation.md
  - docs/en/deployment.md
  - docs/en/api-reference.md
  - docs/en/compliance.md
  - docs/en/faq.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
user_setup: []

must_haves:
  truths:
    - "docs/GLOSSARY.md exists with technical terms and structure for all 8 languages"
    - "docs/TRANSLATION_MANIFEST.md exists with source→target mapping for all 9 source docs × 6 target languages"
    - "3 new English source docs exist in docs/en/ (architecture.md, security.md, operations.md)"
    - "6 language directories exist (fr/, es/, pt/, it/, ar/, nl/) each containing all 9 source docs as copy base"
    - "docs/ar/README.md contains RTL rendering guidance note"
  artifacts:
    - path: "docs/GLOSSARY.md"
      provides: "Technical terminology mapping across all 8 languages"
      min_lines: 30
    - path: "docs/TRANSLATION_MANIFEST.md"
      provides: "Per-file translation status tracking"
      min_lines: 40
    - path: "docs/en/architecture.md"
      provides: "Prose architecture summary (English source for translation)"
      min_lines: 20
    - path: "docs/en/security.md"
      provides: "Security policy document (English source for translation)"
      min_lines: 20
    - path: "docs/en/operations.md"
      provides: "Operations guide (English source for translation)"
      min_lines: 40
    - path: "docs/ar/README.md"
      provides: "Arabic RTL rendering guidance"
      contains: "RTL"
  key_links:
    - from: "docs/TRANSLATION_MANIFEST.md"
      to: "docs/en/*.md"
      via: "Manifest lists each source file path"
      pattern: "docs/en/"
    - from: "docs/GLOSSARY.md"
      to: "docs/TRANSLATION_MANIFEST.md"
      via: "Both reference the same set of source docs and languages"
---

<objective>
Create the translation infrastructure for 6 new language directories (FR, ES, PT, IT, AR, NL) plus glossary, manifest, and English source docs.

**Purpose:** Lay the structural foundation so that Plan 2 can translate content without re-creating directories or negotiating document scope.

**Output:**
- Technical glossary (8 languages)
- Translation manifest (9 source × 6 target)
- 3 new English source docs (architecture, security, operations)
- 6 language directories with source file copies
- Arabic RTL guidance note
</objective>

<execution_context>
@/Users/aljaunia/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/aljaunia/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-documentation-parity/CONTEXT.md
@.planning/v1.5-SPEC.md

# English source docs (6 existing)
@docs/en/getting-started.md
@docs/en/installation.md
@docs/en/deployment.md
@docs/en/api-reference.md
@docs/en/compliance.md
@docs/en/faq.md

# Supplementary source material
@docs/architecture.mmd
@docs/security/incident-response.md
@docs/operations/policy-runbook.md
@docs/operations/slo-runbook.md
@docs/architecture/multimodal.md

# Existing DE translation as pattern reference
@docs/de/getting-started.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create technical glossary and translation manifest</name>
  <files>
    docs/GLOSSARY.md,
    docs/TRANSLATION_MANIFEST.md
  </files>
  <action>
    Create two infrastructure documents per D3 (manifest) and D6 (glossary).

    ---

    **docs/GLOSSARY.md (per D6):**
    Create a markdown table with columns: Term (English) | DE | FR | ES | PT | IT | AR | NL | Notes. Include at minimum these core terms:

    - AnonReq (proper noun — keep in all languages)
    - fail-secure / fail-closed (core invariant concept)
    - tokenization / de-tokenization (core mechanism)
    - Presidio (proper noun — Microsoft PII detection library)
    - PII (Personally Identifiable Information)
    - PHI (Protected Health Information)
    - PCI (Payment Card Industry)
    - anonymization pipeline
    - detection engine
    - classification engine
    - entity / entity type
    - recognizer
    - locale / locale bundle
    - compliance preset
    - SSE streaming / Server-Sent Events
    - provider adapter
    - governance / AI governance
    - RBAC (Role-Based Access Control)
    - DLP (Data Loss Prevention)
    - SOC / SIEM
    - SLO (Service Level Objective)
    - tenant / multi-tenant
    - session scope

    Fill English and DE columns with known terms. Leave FR/ES/PT/IT/AR/NL columns empty (they will be populated during Plan 2 translation). Add a preamble explaining <code>code terms</code> stay untranslated per D7 and that this glossary is the canonical reference for translation consistency.

    ---

    **docs/TRANSLATION_MANIFEST.md (per D3, DOCS-02):**
    Create a tracking table with columns: Source File | FR | ES | PT | IT | AR | NL | Notes.

    Rows for 9 source files:
    - `docs/en/getting-started.md`
    - `docs/en/installation.md`
    - `docs/en/deployment.md`
    - `docs/en/api-reference.md`
    - `docs/en/compliance.md`
    - `docs/en/faq.md`
    - `docs/en/architecture.md`
    - `docs/en/security.md`
    - `docs/en/operations.md`

    In each language cell, use the format: `{status YYYY-MM-DD}` where status is one of `draft` / `reviewed` / `published`. Initial status for all is `draft` with today's date. Add a legend explaining the 3-state review lifecycle and a note that this manifest is updated by Plan 2 as translations complete.

    Per D3: each file gets status, date, reviewer (leave reviewer blank — filled during Plan 2).
  </action>
  <verify>
    <automated>
      test -f docs/GLOSSARY.md && echo "GLOSSARY exists" && \
      test -f docs/TRANSLATION_MANIFEST.md && echo "MANIFEST exists" && \
      grep -q "fail-secure" docs/GLOSSARY.md && echo "Has fail-secure term" && \
      grep -q "getting-started" docs/TRANSLATION_MANIFEST.md && echo "Has getting-started row"
    </automated>
  </verify>
  <done>
    Both docs/GLOSSARY.md and docs/TRANSLATION_MANIFEST.md exist with correct structure. Minimum 20 terms in glossary. Manifest covers all 9 source files × 6 target languages.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create English source documents for architecture, security, and operations</name>
  <files>
    docs/en/architecture.md,
    docs/en/security.md,
    docs/en/operations.md
  </files>
  <action>
    Create 3 new English source documents in docs/en/ that will serve as source for translation. Per D1: "architecture prose summary, security policy, operations guide."

    ---

    **docs/en/architecture.md (architecture prose summary):**
    Read @docs/architecture.mmd (Mermaid diagram spec) and @docs/architecture/multimodal.md for content. Write a prose summary of the AnonReq architecture covering:
    - System overview (gateway sits between app and LLM providers)
    - Request lifecycle: inbound → classification → detection → tokenization → provider adapter → response → restoration → outbound
    - Components: Proxy, Classification Engine, Detection Engine, Tokenization, Restoration, Cache Manager, Provider Adapter
    - Core invariant: raw PII never crosses network boundary
    - Fail-secure behavior: any component failure blocks forwarding
    - Write in plain English prose (not diagram markup) suitable for non-technical evaluators.
    - Keep code blocks, config references, and filenames in English per D7.

    ---

    **docs/en/security.md (security policy):**
    Read @docs/security/incident-response.md. Create a security overview doc covering:
    - Security posture: zero-trust, fail-secure, minimum privilege
    - Anonymization guarantee: PII never leaves the gateway
    - Data handling: ephemeral token mappings, no PII in logs/metrics
    - Incident response summary (reference incident-response.md for details)
    - Keep all config commands and code in English.

    ---

    **docs/en/operations.md (operations guide):**
    Read @docs/operations/policy-runbook.md and @docs/operations/slo-runbook.md. Create a consolidated operations guide covering:
    - Policy management overview (create/update/delete policies)
    - SLO monitoring and dashboards
    - Common operational tasks (restart, cache clear, log inspection)
    - Health check endpoints and metrics
    - Keep all YAML/JSON examples and CLI commands in English per D7.
    - Do NOT duplicate the full content of both runbooks — provide a concise overview with cross-references.
  </action>
  <verify>
    <automated>
      for f in architecture.md security.md operations.md; do \
        test -f "docs/en/$f" && echo "docs/en/$f exists" || echo "MISSING docs/en/$f"; \
      done && \
      grep -q "PII" docs/en/security.md && echo "Security has PII reference" && \
      grep -q "fail-secure\|fail.closed" docs/en/architecture.md && echo "Architecture mentions fail-secure"
    </automated>
  </verify>
  <done>
    All 3 new English source docs exist in docs/en/. Each is substantive prose (not copied Mermaid syntax). Architecture doc explains request lifecycle and fail-secure invariant. Security doc covers data handling guarantees. Operations doc provides consolidated operational overview.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create language directories, copy source files, add Arabic RTL note</name>
  <files>
    docs/fr/,
    docs/es/,
    docs/pt/,
    docs/it/,
    docs/ar/,
    docs/ar/README.md,
    docs/nl/
  </files>
  <action>
    Create 6 language directories and populate them with copies of all 9 English source files as translation bases. Add Arabic RTL guidance per D4.

    ---

    **Language directories (per SPEC §3.1):**
    Create: `docs/fr/`, `docs/es/`, `docs/pt/`, `docs/it/`, `docs/ar/`, `docs/nl/`

    **Copy source files (per D1):**
    Into EACH language directory, copy all 9 English source docs (the 6 existing in docs/en/ plus the 3 created in Task 2):

    - `getting-started.md`
    - `installation.md`
    - `deployment.md`
    - `api-reference.md`
    - `compliance.md`
    - `faq.md`
    - `architecture.md`
    - `security.md`
    - `operations.md`

    For each copied file, add a header line: `> translated from en/{filename}` (matching the pattern used in docs/de/).

    Do NOT translate any content in this task — this is a copy-and-prepare step. Translation happens in Plan 2.

    ---

    **Arabic RTL note (per D4):**
    Create `docs/ar/README.md` with:
    - Statement that this documentation is in Arabic (RTL script)
    - RTL rendering guidance: markdown renderers must support `dir="rtl"` attribute on content blocks; recommends using `<!-- rtl -->` markers if the renderer does not auto-detect
    - Note that code blocks remain LTR (mixed-direction content is expected)
    - Note that full RTL layout verification is deferred (ref: REQUIREMENTS.md v2 deferred items)

    ---

    After this task, the structure should be:
    ```
    docs/
    ├── en/    (9 source files — 6 existing + 3 new)
    ├── de/    (6 existing stub files — unchanged)
    ├── fr/    (9 copied files, untranslated)
    ├── es/    (9 copied files, untranslated)
    ├── pt/    (9 copied files, untranslated)
    ├── it/    (9 copied files, untranslated)
    ├── ar/    (9 copied files + README.md, untranslated)
    ├── nl/    (9 copied files, untranslated)
    ├── GLOSSARY.md
    └── TRANSLATION_MANIFEST.md
    ```
  </action>
  <verify>
    <automated>
      for lang in fr es pt it ar nl; do \
        echo "Checking $lang:" && \
        ls "docs/$lang/"*.md 2>/dev/null | wc -l | xargs echo "  file count:"; \
      done && \
      test -f docs/ar/README.md && echo "AR README exists" && \
      grep -qi "rtl" docs/ar/README.md && echo "AR README has RTL note"
    </automated>
  </verify>
  <done>
    6 language directories exist. Each contains exactly 9 markdown files (copies of English sources with translated-from header). docs/ar/README.md has RTL rendering guidance. No translated content yet — that is Plan 2's responsibility.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source docs → translated docs | Human-readable content — no sensitive data in docs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | docs/*.md | mitigate | Code/config blocks remain in English (D7) to prevent broken copy-paste |
| T-25-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — pure content work |
</threat_model>

<verification>
- docs/GLOSSARY.md has ≥ 20 technical terms with English + DE columns filled
- docs/TRANSLATION_MANIFEST.md has 9 source rows × 6 target language columns
- 3 new English source docs (docs/en/architecture.md, security.md, operations.md) exist and are substantive
- 6 language directories each contain 9 file copies
- docs/ar/README.md contains RTL rendering guidance
</verification>

<success_criteria>
Global enterprises can begin reading documentation in their preferred language because all 6 language directories exist with source file copies, a glossary standardizes key terms, the manifest tracks status, and the Arabic directory has RTL guidance.
</success_criteria>

<output>
Create `.planning/phases/25-documentation-parity/25-01-SUMMARY.md` when done
</output>
