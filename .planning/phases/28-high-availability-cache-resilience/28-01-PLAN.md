---
phase: 28-high-availability-cache-resilience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - src/anonreq/cache/manager.py
  - tests/test_cache.py
autonomous: false
requirements:
  - HA-01
user_setup:
  - "Approve Tenacity after reviewing its PyPI publisher, source repository, and license; research marked the package legitimacy audit as SUS despite established provenance."
must_haves:
  truths:
    - "A configured redis:// or rediss:// URL constructs the existing standalone asyncio Valkey client."
    - "A configured redis+sentinel://host:port,host:port/service URL constructs a Sentinel primary client, and a configured redis+cluster://host:port,host:port URL constructs an asyncio cluster client."
    - "Malformed, empty, or unsupported HA topology URLs fail during CacheManager construction without attempting a network connection."
  artifacts:
    - path: src/anonreq/cache/manager.py
      provides: "Validated D-01 URL parser and standalone/Sentinel/Cluster CacheManager client factory"
      contains: "redis+sentinel"
    - path: tests/test_cache.py
      provides: "Factory and malformed-topology unit coverage using patched redis-py constructors"
      contains: "redis+cluster"
    - path: pyproject.toml
      provides: "Approved runtime Tenacity declaration"
      contains: "tenacity"
    - path: uv.lock
      provides: "Resolver-generated Tenacity lock record"
      contains: "name = \"tenacity\""
  key_links:
    - from: src/anonreq/cache/manager.py
      to: ANONREQ_VALKEY_URL
      via: "CacheManager.__init__ parses the D-01 custom scheme and selects one redis.asyncio backend"
      pattern: "redis\\+(sentinel|cluster)"
    - from: src/anonreq/cache/manager.py
      to: redis.asyncio.sentinel.Sentinel
      via: "validated sentinel host tuples and service name are passed to Sentinel(...).master_for(...)"
      pattern: "master_for"
---

<objective>
Install the approved retry dependency and make `CacheManager` topology-aware while preserving its public mapping API and lifetime ownership.

Purpose: HA URL schemes must select official redis-py async backends deterministically before any request can use a cache mapping.
Output: An audited Tenacity runtime dependency, a resolver-generated lockfile update, and tested standalone/Sentinel/Cluster factory selection in `CacheManager`.
</objective>

<execution_context>
@/Users/aljaunia/.codex/gsd-core/workflows/execute-plan.md
@/Users/aljaunia/.codex/gsd-core/templates/summary.md
@/Users/aljaunia/.codex/gsd-core/references/checkpoints.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-high-availability-cache-resilience/28-CONTEXT.md
@.planning/phases/28-high-availability-cache-resilience/28-RESEARCH.md
@.planning/phases/28-high-availability-cache-resilience/28-PATTERNS.md
@.planning/phases/28-high-availability-cache-resilience/28-VALIDATION.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@src/anonreq/cache/manager.py
@tests/test_cache.py
@pyproject.toml
</context>

<phase_scope>
In scope: D-01 and D-02 topology parsing and redis-py client construction, plus the mandated dependency approval/install path. Out of scope: retry execution, HTTP exception translation, health/readiness policy, Helm, tenant-key migration, and new configuration settings. Do not add environment variables; `ANONREQ_VALKEY_URL` is the sole topology input.
</phase_scope>

<artifacts-this-phase-produces>
- A `CacheManager` private factory/parser that accepts only the locked standalone, Sentinel, and Cluster URL forms.
- A locked Tenacity dependency for the later bounded retry plan, added only after a human legitimacy approval.
- Unit tests proving constructor selection and immediate rejection of malformed topology strings without credential or URL logging.
</artifacts-this-phase-produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Approve Tenacity package legitimacy before installation</name>
  <read_first>.planning/phases/28-high-availability-cache-resilience/28-RESEARCH.md, pyproject.toml, uv.lock</read_first>
  <action>Present the Phase 28 package-legitimacy finding for `tenacity`: it is required by locked D-04, is an established PyPI package with source repository `github.com/jd/tenacity`, but the research seam classified it SUS because registry metadata could not be resolved. Ask the human to approve this specific dependency before any `uv add tenacity` or lockfile change. Do not substitute a hand-written retry loop or redis-py's internal retry mechanism, because D-04 requires Tenacity.</action>
  <verify><automated>test -f pyproject.toml && ! rg -q '^\s*"tenacity' pyproject.toml</automated></verify>
  <acceptance_criteria>
    - "A human has explicitly approved adding Tenacity, or has rejected it and execution remains paused."
    - "No package metadata is changed before the approval signal."
  </acceptance_criteria>
  <done>Human legitimacy approval is recorded in the executor summary and the plan may resume to dependency installation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Tenacity and implement validated Valkey topology factories</name>
  <files>pyproject.toml, uv.lock, src/anonreq/cache/manager.py, tests/test_cache.py</files>
  <read_first>src/anonreq/cache/manager.py, tests/test_cache.py, .planning/phases/28-high-availability-cache-resilience/28-PATTERNS.md, .planning/research/ARCHITECTURE.md</read_first>
  <behavior>
    - Test accepted standalone `redis://` and `rediss://` URLs select `redis.asyncio.from_url` with the existing response decoding, health interval, and connect timeout options.
    - Test the exact D-01 Sentinel URL grammar produces parsed `(host, port)` seed tuples and invokes `Sentinel(...).master_for(service_name, ...)`.
    - Test the exact D-01 Cluster URL grammar produces validated startup nodes and invokes `redis.asyncio.cluster.RedisCluster`.
    - Test unsupported schemes, blank host entries, invalid ports, missing Sentinel service names, and empty Cluster seed lists raise a local validation error before a client factory is invoked.
  </behavior>
  <action>After Task 1 approval, run `uv add tenacity` and regenerate `uv.lock`; add it to `[project].dependencies`, never a dev-only extra. In `src/anonreq/cache/manager.py`, implement a private parsed-topology value object or helper using stdlib URL parsing and explicit comma-separated authority validation, per D-01 and D-02. Accept only `redis://`, `rediss://`, `redis+sentinel://`, and `redis+cluster://`; retain database-path/query support only for the standalone forms. For Sentinel, require a non-empty one-segment service path and construct `redis.asyncio.sentinel.Sentinel` then `master_for(service_name, decode_responses=True, health_check_interval=5, socket_connect_timeout=3)`. For Cluster, require one or more `host:port` seeds and construct `redis.asyncio.cluster.RedisCluster` from those parsed startup nodes with the same safe connection defaults supported by redis-py 8.0.1. Preserve `CacheManager` public methods, `_ttl`, `close()`, and the existing `anonreq:{tenant_id}:{session_id}` key contract. Do not log source URLs, node lists, service names, cache values, or parser exception text. Extend `tests/test_cache.py` using patched imported factory symbols and injected mocks, following its fakeredis fixture style; do not require a live Sentinel or Cluster. Cite D-01 and D-02 in implementation comments/docstrings only where that project's conventions require traceability.</action>
  <verify><automated>uv run pytest tests/test_cache.py -q</automated></verify>
  <acceptance_criteria>
    - "`pyproject.toml` declares Tenacity as a runtime dependency and `uv.lock` contains its resolver-generated package record."
    - "`CacheManager.__init__` accepts the two standalone and two custom D-01 scheme families and rejects any other scheme locally."
    - "Sentinel factory tests assert `master_for` receives the URL service name; Cluster factory tests assert all comma-separated nodes are supplied to the cluster constructor."
    - "Malformed topology tests prove factory mocks are not called."
  </acceptance_criteria>
  <done>`uv run pytest tests/test_cache.py -q` passes with topology selection and malformed-input coverage, and no new settings/configuration variable exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `ANONREQ_VALKEY_URL` configuration -> CacheManager parser | Deployment-controlled string selects a privileged network client topology. |
| CacheManager -> Valkey Sentinel/Cluster | Cache holds ephemeral token mappings required to prevent un-anonymized forwarding. |
| PyPI/lock resolver -> runtime dependency | Retry library code becomes part of the gateway's trusted execution path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | topology URL parser | mitigate | Allowlist the four D-01 schemes; validate every host, port, node, and Sentinel service before a factory call. |
| T-28-02 | Information Disclosure | constructor diagnostics | mitigate | Do not log connection URLs, node lists, service names, credentials, or cache values. |
| T-28-SC | Tampering | Tenacity supply chain | mitigate | Require the human legitimacy checkpoint before `uv add`; lock via uv rather than hand-editing package metadata. |
</threat_model>

<verification>
1. Run `uv run pytest tests/test_cache.py -q`.
2. Run `uv run python -c "from anonreq.cache.manager import CacheManager; CacheManager('redis://localhost:6379/0'); print('OK')"` without contacting Valkey.
3. Inspect `git diff -- pyproject.toml uv.lock src/anonreq/cache/manager.py tests/test_cache.py` to confirm no URL or mapping values were added to logging.
</verification>

<success_criteria>
1. The four locked D-01 URL families route to their corresponding official redis-py asyncio constructors.
2. Invalid HA topology configuration fails immediately and deterministically rather than silently selecting standalone Redis.
3. The retry dependency is human-approved, declared at runtime, and lockfile-resolved.
</success_criteria>

<output>
Create `.planning/phases/28-high-availability-cache-resilience/28-01-SUMMARY.md` when done.
</output>
