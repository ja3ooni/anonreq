---
phase: 01-foundation-fail-secure-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - requirements.txt
  - requirements-dev.txt
  - .env.example
  - config/providers.yaml
  - src/anonreq/__init__.py
  - src/anonreq/__about__.py
  - src/anonreq/models/__init__.py
  - src/anonreq/config.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_config.py
autonomous: true
requirements:
  - DOCK-05
  - DOCK-06
  - DOCK-07
  - FAIL-04
  - FAIL-01

user_setup: []

must_haves:
  truths:
    - "Operator can configure all gateway settings via environment variables"
    - "Missing required env var causes clear startup failure message"
    - "All deps are pinned in requirements files for reproducible builds"
    - "Shared test fixtures exist for all downstream Phase 1 plans"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project metadata, build system, dependency declarations"
      contains: "name = \"anonreq\""
    - path: "src/anonreq/config.py"
      provides: "Pydantic Settings with env var loading + validation"
      exports: ["Settings", "settings"]
      min_lines: 30
    - path: ".env.example"
      provides: "Documentation for every required and optional env var"
      contains: "ANONREQ_API_KEY"
    - path: "tests/conftest.py"
      provides: "Shared fixtures: async test client, settings override"
      min_lines: 15
    - path: "tests/test_config.py"
      provides: "Config loading and validation tests"
      contains: "test_"
  key_links:
    - from: "src/anonreq/config.py"
      to: "pyproject.toml"
      via: "pydantic-settings dependency declared in pyproject.toml"
      pattern: "pydantic"
    - from: "tests/test_config.py"
      to: "src/anonreq/config.py"
      via: "imports settings, tests env var loading and validation"
      pattern: "from anonreq.config import"
---

<objective>
Establish the Python project scaffold and configuration management layer for the AnonReq gateway. This plan creates the build system (pyproject.toml), pinned dependency files, package layout under `src/anonreq/`, the hybrid Pydantic Settings + YAML configuration module (per D-07), and the shared test infrastructure that all subsequent Phase 1 plans depend on.

Purpose: Without this scaffold, no other plan can install dependencies, import modules, or run tests. Every downstream plan (01-02 through 01-04) requires this plan to complete first.

Output: A working Python project with `python -c "from anonreq.config import settings"` succeeding after `pip install -e .` and `pytest tests/test_config.py -x` passing.
</objective>

<execution_context>
@/Users/aljaunia/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/aljaunia/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-fail-secure-auth/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create project scaffold — pyproject.toml, package layout, dependency files</name>
  <files>
    pyproject.toml,
    requirements.txt,
    requirements-dev.txt,
    .env.example,
    config/providers.yaml,
    src/anonreq/__init__.py,
    src/anonreq/__about__.py,
    src/anonreq/models/__init__.py,
    tests/__init__.py
  </files>
  <action>
    Create the AnonReq project scaffold under `/Users/aljaunia/Documents/Develop/annon/`:

    1. **pyproject.toml** — setuptools build system, name=anonreq, version=0.1.0, requires-python>=3.12, Apache-2.0 license. Declare dependencies:
       - Production: fastapi>=0.138.0, uvicorn[standard]>=0.49.0, pydantic-settings>=2.14.2, structlog>=26.1.0, python-json-logger>=4.1.0, redis>=8.0.0, pyyaml>=6.0.3, httpx>=0.28.1, prometheus-client>=0.25.0
       - Dev extras: pytest>=9.0, pytest-asyncio>=1.4.0, hypothesis>=6.155.0, respx>=0.21.0
       Use `[tool.setuptools.packages.find]` with `where = ["src"]` and `include = ["anonreq*"]`. Configure `[tool.pytest.ini_options]` with `asyncio_mode = "auto"`, `testpaths = ["tests"]`, `pythonpath = ["src"]`. Add `[tool.coverage.run]` with `source = ["anonreq"]`.

    2. **requirements.txt** — pinned production deps (exact versions matching pyproject.toml).

    3. **requirements-dev.txt** — pinned dev deps.

    4. **src/anonreq/__init__.py** — empty init (or just `"""AnonReq — AI Security Gateway."""`).

    5. **src/anonreq/__about__.py** — `__version__ = "0.1.0"`.

    6. **src/anonreq/models/__init__.py** — empty init for the models package.

    7. **.env.example** — document ALL env vars per D-08 and D-09:
       ```
       ANONREQ_API_KEY=<your-api-key-min-32-chars>
       ANONREQ_VALKEY_URL=redis://valkey:6379/0
       ANONREQ_PRESIDIO_URL=http://presidio-analyzer:5001
       ANONREQ_HOST=0.0.0.0
       ANONREQ_PORT=8080
       ANONREQ_LOG_LEVEL=INFO
       ANONREQ_REQUEST_TIMEOUT_SECONDS=30
       ```

    8. **config/providers.yaml** — empty stub for the YAML-based provider capability registry (D-16):
       ```yaml
       # Provider capability registry — loaded at startup
       # Populated in Phase 3 with provider definitions
       providers: {}
       ```

    9. **tests/__init__.py** — empty init.

    Do NOT write any implementation code yet. This task is pure project structure.
  </action>
  <verify>
    <automated>
      python3 -c "import ast; ast.parse(open('pyproject.toml').read())" 2>&1 || true;
      python3 -c "open('src/anonreq/__init__.py').read()" 2>&1;
      ls src/anonreq/__about__.py src/anonreq/config.py 2>&1 || echo "config.py pending Task 2";
      grep -q 'name = "anonreq"' pyproject.toml && echo "pyproject.toml valid" || echo "pyproject.toml missing name"
    </automated>
  </verify>
  <done>All 9 scaffold files exist with correct content. pyproject.toml declares production and dev deps. .env.example documents all 7 env vars. config/providers.yaml exists as stub.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement configuration module with Pydantic Settings + YAML</name>
  <files>
    src/anonreq/config.py,
    tests/conftest.py,
    tests/test_config.py
  </files>
  <behavior>
    - Test 1: Settings loads from explicit env vars with correct types
    - Test 2: ANONREQ_API_KEY with length &lt; 32 raises ValidationError
    - Test 3: Missing required env var raises ValidationError with clear message
    - Test 4: Optional env vars use documented defaults
    - Test 5: unknown env vars are silently ignored (extra="ignore")
    - Test 6: VALKEY_URL and PRESIDIO_URL accept valid URL strings
  </behavior>
  <action>
    Implement the hybrid configuration module per D-07, D-08, D-09, D-10.

    1. **src/anonreq/config.py:**
       - `Settings(BaseSettings)` with `model_config = SettingsConfigDict(env_prefix="ANONREQ_", yaml_file="config/providers.yaml", extra="ignore")`.
       - Required fields:
         - `API_KEY: str = Field(min_length=32, validation_alias="ANONREQ_API_KEY")`
         - `VALKEY_URL: str = Field(validation_alias="ANONREQ_VALKEY_URL")`
         - `PRESIDIO_URL: str = Field(validation_alias="ANONREQ_PRESIDIO_URL")`
       - Optional fields with defaults:
         - `HOST: str = "0.0.0.0"`
         - `PORT: int = 8080`
         - `LOG_LEVEL: str = "INFO"`
         - `REQUEST_TIMEOUT_SECONDS: int = 30`
       - Add `@field_validator("API_KEY")` classmethod that raises ValueError if `len(v) < 32`.
       - Module-level singleton: `settings = Settings()` (will raise on import if required vars missing — per D-10 startup validation).
       - Per D-16: add a `load_provider_registry()` function that loads `config/providers.yaml` via `yaml.safe_load()` and returns a dict.

    2. **tests/conftest.py** — shared fixtures for all downstream plans:
       - `settings_override` fixture that monkeypatches env vars before Settings import.
       - `test_client` fixture that creates a FastAPI TestClient (will be used by plans 01-03 and 01-04).
       - Import from `httpx` for `AsyncClient` if needed for async tests.

    3. **tests/test_config.py:**
       - Test that all three required env vars produce a valid Settings instance.
       - Test that short API_KEY (&lt; 32 chars) raises ValidationError.
       - Test that missing each required var individually raises ValidationError.
       - Test that optional fields have correct defaults.
       - Test that unknown env vars do not raise errors (extra="ignore").
       - Use `monkeypatch` or `pytest.mark.parametrize` for the missing-var tests.
  </action>
  <verify>
    <automated>
      ANONREQ_API_KEY=$(python3 -c "import secrets; print(secrets.token_hex(20))") \
      ANONREQ_VALKEY_URL=redis://localhost:6379/0 \
      ANONREQ_PRESIDIO_URL=http://localhost:5001 \
      python3 -c "from anonreq.config import settings; print(settings.API_KEY[:8]+'...', settings.HOST, settings.PORT)" 2>&1;
      echo "---";
      ANONREQ_API_KEY=short ANONREQ_VALKEY_URL=redis://localhost:6379 ANONREQ_PRESIDIO_URL=http://localhost:5001 \
      python3 -c "from anonreq.config import settings" 2>&1 | grep -q "validation" && echo "Short key correctly rejected" || echo "check"
    </automated>
  </verify>
  <done>config.py loads env vars correctly. Validation rejects short API keys, missing required vars, and silently ignores unknown vars. conftest.py provides shared fixtures. Config tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Env var injection | Untrusted input crosses into config at process start |
| YAML file loading | Untrusted YAML file parsed at startup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | config.py env var loading | mitigate | Pydantic Settings `extra="ignore"` drops unknown env vars; `field_validator` enforces API_KEY min length per D-10 |
| T-01-01-02 | Elevation of Privilege | config/providers.yaml | mitigate | `yaml.safe_load()` prevents arbitrary code execution from YAML (not `yaml.load()`) |
| T-01-01-SC | Tampering | pip installs (pyproject.toml deps) | mitigate | All packages verified in RESEARCH.md § Package Legitimacy Audit — approved [SUS] verdicts, no [SLOP] found |
</threat_model>

<verification>
1. `pip install -e ".[dev]"` completes without errors
2. `ANONREQ_API_KEY... python3 -c "from anonreq.config import settings"` succeeds
3. `pytest tests/test_config.py -x --tb=short` passes
4. All 12 files created with correct content
</verification>

<success_criteria>
- [ ] pyproject.toml declares all Phase 1 dependencies with correct versions
- [ ] config.py loads env vars with validation, rejects short API keys and missing required vars
- [ ] YAML safe_load prevents code injection from providers.yaml
- [ ] .env.example documents all 7 env vars (3 required, 4 optional)
- [ ] conftest.py provides settings_override and test_client fixtures
- [ ] `pytest tests/test_config.py -x` passes with 6+ test cases
- [ ] All files committed to git
</success_criteria>
