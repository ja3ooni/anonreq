---
phase: 22-close-milestone-audit-gaps-runtime-integration-blockers
plan: 01
type: execute
gap_closure: true
wave: 1
depends_on: []
files_modified:
  - src/anonreq/main.py
  - tests/integration/test_app_runtime_wiring.py
  - tests/test_soc_runtime_wiring.py
autonomous: true
requirements:
  - MULTI-05
  - APPL-CDP-01
  - APPL-DISC-04
  - APPL-SOC-01
  - APPL-SOC-02
  - APPL-SOC-03
  - APPL-SOC-04
  - APPL-SOC-05
  - APPL-SOC-06
  - APPL-SOC-07
  - APPL-SOC-08
  - APPL-SOC-09
must_haves:
  truths:
    - "Unsupported HTTP content types are rejected by the real FastAPI app with HTTP 415 before route or provider work."
    - "Discovery inventory admin endpoints are reachable from create_app() and use app.state.inventory_service."
    - "SOCNormalizer delivers normalized metadata-only events to SinkRouter.fan_out through a registered callback."
  artifacts:
    - path: "src/anonreq/main.py"
      provides: "ContentTypeMiddleware installation, discovery router inclusion, inventory service state, SOC normalizer callback wiring"
      contains: "ContentTypeMiddleware"
    - path: "tests/integration/test_app_runtime_wiring.py"
      provides: "FastAPI app wiring regression tests for content-type and discovery runtime registration"
    - path: "tests/test_soc_runtime_wiring.py"
      provides: "SOC normalizer to sink router fan-out regression test"
  key_links:
    - from: "src/anonreq/main.py"
      to: "src/anonreq/middleware/content_type.py"
      via: "app.add_middleware(ContentTypeMiddleware, dispatcher=ContentTypeDispatcher(...))"
      pattern: "ContentTypeMiddleware"
    - from: "src/anonreq/main.py"
      to: "src/anonreq/discovery/admin_router.py"
      via: "app.include_router(discovery_admin_router)"
      pattern: "discovery.*admin"
    - from: "src/anonreq/main.py"
      to: "src/anonreq/soc/normalizer.py"
      via: "soc_normalizer.register_sink_callback"
      pattern: "register_sink_callback"
---

<objective>
Close the low-risk app installation blockers from the milestone audit: multimodal content-type middleware, discovery inventory admin router registration, and SOC normalizer-to-SIEM sink fan-out.

Purpose: These components already exist, but audit evidence shows they are not installed in the real FastAPI runtime path. The plan wires existing modules at their current seams without adding new external dependencies or rewriting the app factory.
Output: Runtime app wiring and focused tests proving HTTP 415 rejection, discovery route reachability, and SOC event fan-out.
</objective>

<execution_context>
@/Users/aljaunia/.codex/gsd-core/workflows/execute-plan.md
@/Users/aljaunia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/22-close-milestone-audit-gaps-runtime-integration-blockers/22-CONTEXT.md
@.planning/phases/22-close-milestone-audit-gaps-runtime-integration-blockers/22-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/codebase/INTEGRATIONS.md
@src/anonreq/main.py
@src/anonreq/middleware/content_type.py
@src/anonreq/multimodal/dispatcher.py
@src/anonreq/discovery/admin_router.py
@src/anonreq/discovery/inventory.py
@src/anonreq/soc/normalizer.py
@src/anonreq/soc/router.py
@src/anonreq/soc/sink_factory.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove app-level content-type and discovery gaps</name>
  <files>tests/integration/test_app_runtime_wiring.py</files>
  <behavior>
    - Test 1: A request with an unsupported Content-Type sent through create_app() returns HTTP 415 and does not invoke /v1/chat/completions.
    - Test 2: The app route table contains GET /v1/admin/discovery/inventory.
    - Test 3: With app.state.inventory_service set to AssetInventory, GET /v1/admin/discovery/inventory returns JSON records and CSV export returns text/csv.
  </behavior>
  <action>Add focused ASGITransport tests using create_app() and existing auth test patterns. Patch startup dependencies only as needed to avoid live Valkey, Presidio, database, SIEM, or network listeners. Keep request bodies synthetic and assert no raw PII appears in response bodies or logs. These tests cover MULTI-05, APPL-CDP-01, and APPL-DISC-04.</action>
  <verify>
    <automated>uv run pytest tests/integration/test_app_runtime_wiring.py -q</automated>
  </verify>
  <done>The test file fails before runtime wiring and passes after Task 2: unsupported media types return 415, discovery inventory route exists, and JSON/CSV inventory responses use AssetInventory.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install middleware and discovery inventory runtime wiring</name>
  <files>src/anonreq/main.py, tests/integration/test_app_runtime_wiring.py</files>
  <behavior>
    - Test 1: ContentTypeMiddleware is installed with a ContentTypeDispatcher using existing JSON and multipart analyzers.
    - Test 2: app.state.inventory_service is initialized to AssetInventory during lifespan.
    - Test 3: discovery_admin_router is included in create_app() without removing existing auth protection.
  </behavior>
  <action>In create_app(), import and add the existing ContentTypeMiddleware with a real ContentTypeDispatcher built from existing multimodal analyzers. Initialize app.state.inventory_service with AssetInventory during lifespan near existing Phase 17/19 discovery setup. Include the existing discovery admin router in the route section. Preserve existing middleware order intent: content-type rejection must occur before chat route/provider work; request_id middleware must still populate request.state for downstream handlers. Do not create duplicate discovery endpoints or duplicate content-type policy logic.</action>
  <verify>
    <automated>uv run pytest tests/integration/test_app_runtime_wiring.py -q</automated>
  </verify>
  <done>create_app() rejects unsupported content types with HTTP 415 and exposes /v1/admin/discovery/inventory backed by app.state.inventory_service.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire SOC normalizer fan-out to configured sinks</name>
  <files>src/anonreq/main.py, tests/test_soc_runtime_wiring.py</files>
  <behavior>
    - Test 1: When both SOCNormalizer and SinkRouter are initialized, main.py registers sink_router.fan_out as a callback on the normalizer.
    - Test 2: Publishing a raw security event through the normalizer results in one normalized event delivered to a fake sink router.
    - Test 3: The delivered normalized event contains metadata fields only and excludes raw content, prompt, body, arguments, and result values.
  </behavior>
  <action>After sink router creation in the Phase 20-05 lifespan block, register app.state.soc_sink_router.fan_out with app.state.soc_normalizer using register_sink_callback. Keep sink startup/health behavior unchanged and let SinkRouter isolate per-sink delivery failures. Add a direct SOC wiring test around SOCNormalizer and a fake async fan_out callback; do not forward raw events to sinks before normalization. This covers APPL-SOC-01 through APPL-SOC-09.</action>
  <verify>
    <automated>uv run pytest tests/test_soc_runtime_wiring.py -q</automated>
  </verify>
  <done>Normalized SOC events fan out to the configured sink router, and the test proves no raw event content reaches the sink callback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client to FastAPI middleware | Untrusted HTTP headers and bodies enter content-type enforcement. |
| admin client to discovery API | Authenticated admin request reads inventory metadata. |
| normalizer to SIEM sinks | Internal normalized security metadata leaves AnonReq toward external SIEM systems. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01-01 | Information Disclosure | ContentTypeMiddleware/main.py | mitigate | Reject unsupported content types before chat/provider work and assert response body contains only structured error metadata. |
| T-22-01-02 | Tampering | discovery admin router | mitigate | Keep existing auth_context dependency and initialize only AssetInventory metadata service, not raw traffic stores. |
| T-22-01-03 | Information Disclosure | SOCNormalizer to SinkRouter | mitigate | Register fan-out only after SOCNormalizer normalization and test that forbidden raw-content fields are absent. |
| T-22-01-04 | Denial of Service | SOC sink fan-out | accept | SinkRouter already isolates sink failures and uses health/backoff behavior; this plan only connects the existing callback. |
| T-22-01-SC | Tampering | package installs | mitigate | No package-manager installs are allowed in this plan. |
</threat_model>

<verification>
Run the targeted tests first, then the adjacent existing suites:

<automated>uv run pytest tests/integration/test_app_runtime_wiring.py tests/test_soc_runtime_wiring.py -q</automated>
<automated>uv run pytest tests/multimodal/test_dispatcher.py tests/discovery/test_discovery_inventory.py tests/test_soc_normalizer.py tests/test_soc_api.py -q</automated>
</verification>

<success_criteria>
MULTI-05, APPL-CDP-01, APPL-DISC-04, and APPL-SOC-01 through APPL-SOC-09 have runtime evidence through create_app() or SOCNormalizer callback tests. The implementation uses existing modules, preserves auth and metadata-only constraints, and does not introduce new dependencies.
</success_criteria>

<output>
Create `.planning/phases/22-close-milestone-audit-gaps-runtime-integration-blockers/22-01-SUMMARY.md` when done.
</output>
